========== ./composer.json ==========
{
    "name": "zayne/zayneui",
    "description": "CSS token-driven UI component engine for Laravel",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": "^8.1",
        "illuminate/support": "^10.0|^11.0|^12.0|^13.0",
        "illuminate/view": "^10.0|^11.0|^12.0|^13.0"
    },
    "autoload": {
        "psr-4": {
            "Zayne\\UI\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Zayne\\UI\\ZayneServiceProvider"
            ],
            "aliases": {
                "Zayne": "Zayne\\UI\\Zayne"
            }
        }
    }
}

========== ./README.md ==========
# Zayne UI

Zayne UI is a CSS token-driven component engine for Laravel + Blade.

```blade
<head>
    @zayneAppearance
    @zayneStyles
</head>
<body>
    <zayne:button variant="solid" color="primary">Save</zayne:button>
    @zayneScripts
</body>
```

========== ./src/Components/Alert.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Alert extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'soft',
        public string $color = 'info',
        public string $padding = '1rem',
        public string $radius = 'var(--zayne-radius-box)',
        public ?string $shadow = null,
        public string $gap = '0.75rem',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'solid' => [
                'danger' => ['background' => 'var(--zayne-color-danger)', 'color' => 'var(--zayne-color-danger-content)'],
                'success' => ['background' => 'var(--zayne-color-success)', 'color' => 'var(--zayne-color-success-content)'],
                'warning' => ['background' => 'var(--zayne-color-warning)', 'color' => 'var(--zayne-color-warning-content)'],
                'info' => ['background' => 'var(--zayne-color-info)', 'color' => 'var(--zayne-color-info-content)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'danger' => ['background' => 'color-mix(in oklch, var(--zayne-color-danger) 14%, transparent)', 'color' => 'var(--zayne-color-danger)'],
                'success' => ['background' => 'color-mix(in oklch, var(--zayne-color-success) 14%, transparent)', 'color' => 'var(--zayne-color-success)'],
                'warning' => ['background' => 'color-mix(in oklch, var(--zayne-color-warning) 18%, transparent)', 'color' => 'var(--zayne-color-warning)'],
                'info' => ['background' => 'color-mix(in oklch, var(--zayne-color-info) 16%, transparent)', 'color' => 'var(--zayne-color-info)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'outline' => [
                'danger' => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)', 'border' => '1px solid var(--zayne-color-danger)'],
                'success' => ['background' => 'transparent', 'color' => 'var(--zayne-color-success)', 'border' => '1px solid var(--zayne-color-success)'],
                'warning' => ['background' => 'transparent', 'color' => 'var(--zayne-color-warning)', 'border' => '1px solid var(--zayne-color-warning)'],
                'info' => ['background' => 'transparent', 'color' => 'var(--zayne-color-info)', 'border' => '1px solid var(--zayne-color-info)'],
                'base' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'border' => '1px solid var(--zayne-color-base-border)'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['soft']['info'];

        $this->style = Zayne::styleString(array_merge([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'gap' => $this->gap,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::alert');
    }
}

========== ./src/Components/Avatar.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Avatar extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'soft',
        public string $color = 'base',
        public string $size = 'md',
        public ?string $src = null,
        public string $alt = 'Avatar',
        public string $radius = '999px',
        public ?string $shadow = null,
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'solid' => [
                'primary' => ['background' => 'var(--zayne-color-primary)', 'color' => 'var(--zayne-color-primary-content)'],
                'secondary' => ['background' => 'var(--zayne-color-secondary)', 'color' => 'var(--zayne-color-secondary-content)'],
                'base' => ['background' => 'var(--zayne-color-base-300)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 18%, transparent)', 'color' => 'var(--zayne-color-primary)'],
                'secondary' => ['background' => 'color-mix(in oklch, var(--zayne-color-secondary) 18%, transparent)', 'color' => 'var(--zayne-color-secondary)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['soft']['base'];

        $this->style = Zayne::styleString(array_merge([
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::avatar');
    }
}

========== ./src/Components/Badge.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Badge extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'soft',
        public string $color = 'base',
        public string $size = 'md',
        public ?string $padding = null,
        public string $radius = 'var(--zayne-radius-selector)',
        public ?string $shadow = null,
        public string $gap = '0.25rem',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'solid' => [
                'primary' => ['background' => 'var(--zayne-color-primary)', 'color' => 'var(--zayne-color-primary-content)'],
                'secondary' => ['background' => 'var(--zayne-color-secondary)', 'color' => 'var(--zayne-color-secondary-content)'],
                'danger' => ['background' => 'var(--zayne-color-danger)', 'color' => 'var(--zayne-color-danger-content)'],
                'success' => ['background' => 'var(--zayne-color-success)', 'color' => 'var(--zayne-color-success-content)'],
                'warning' => ['background' => 'var(--zayne-color-warning)', 'color' => 'var(--zayne-color-warning-content)'],
                'info' => ['background' => 'var(--zayne-color-info)', 'color' => 'var(--zayne-color-info-content)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 18%, transparent)', 'color' => 'var(--zayne-color-primary)'],
                'secondary' => ['background' => 'color-mix(in oklch, var(--zayne-color-secondary) 18%, transparent)', 'color' => 'var(--zayne-color-secondary)'],
                'danger' => ['background' => 'color-mix(in oklch, var(--zayne-color-danger) 18%, transparent)', 'color' => 'var(--zayne-color-danger)'],
                'success' => ['background' => 'color-mix(in oklch, var(--zayne-color-success) 18%, transparent)', 'color' => 'var(--zayne-color-success)'],
                'warning' => ['background' => 'color-mix(in oklch, var(--zayne-color-warning) 18%, transparent)', 'color' => 'var(--zayne-color-warning)'],
                'info' => ['background' => 'color-mix(in oklch, var(--zayne-color-info) 18%, transparent)', 'color' => 'var(--zayne-color-info)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'outline' => [
                'primary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)', 'border' => '1px solid var(--zayne-color-primary)'],
                'secondary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-secondary)', 'border' => '1px solid var(--zayne-color-secondary)'],
                'danger' => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)', 'border' => '1px solid var(--zayne-color-danger)'],
                'success' => ['background' => 'transparent', 'color' => 'var(--zayne-color-success)', 'border' => '1px solid var(--zayne-color-success)'],
                'warning' => ['background' => 'transparent', 'color' => 'var(--zayne-color-warning)', 'border' => '1px solid var(--zayne-color-warning)'],
                'info' => ['background' => 'transparent', 'color' => 'var(--zayne-color-info)', 'border' => '1px solid var(--zayne-color-info)'],
                'base' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'border' => '1px solid var(--zayne-color-base-border)'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['soft']['base'];

        $base = [
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'gap' => $this->gap,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ];

        $this->style = Zayne::styleString(array_merge($base, $resolved));
    }

    public function render(): View
    {
        return view('zayne::badge');
    }
}

========== ./src/Components/Button.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Button extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'solid',
        public string $color = 'primary',
        public string $size = 'md',
        public ?string $href = null,
        public ?string $padding = null,
        public string $radius = 'var(--zayne-radius-field)',
        public ?string $shadow = null,
        public string $gap = 'var(--zayne-gap-button)',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'solid' => [
                'primary' => ['background' => 'var(--zayne-color-primary)', 'color' => 'var(--zayne-color-primary-content)'],
                'secondary' => ['background' => 'var(--zayne-color-secondary)', 'color' => 'var(--zayne-color-secondary-content)'],
                'danger' => ['background' => 'var(--zayne-color-danger)', 'color' => 'var(--zayne-color-danger-content)'],
                'success' => ['background' => 'var(--zayne-color-success)', 'color' => 'var(--zayne-color-success-content)'],
                'warning' => ['background' => 'var(--zayne-color-warning)', 'color' => 'var(--zayne-color-warning-content)'],
                'info' => ['background' => 'var(--zayne-color-info)', 'color' => 'var(--zayne-color-info-content)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 15%, transparent)', 'color' => 'var(--zayne-color-primary)'],
                'secondary' => ['background' => 'color-mix(in oklch, var(--zayne-color-secondary) 15%, transparent)', 'color' => 'var(--zayne-color-secondary)'],
                'danger' => ['background' => 'color-mix(in oklch, var(--zayne-color-danger) 15%, transparent)', 'color' => 'var(--zayne-color-danger)'],
                'success' => ['background' => 'color-mix(in oklch, var(--zayne-color-success) 15%, transparent)', 'color' => 'var(--zayne-color-success)'],
                'warning' => ['background' => 'color-mix(in oklch, var(--zayne-color-warning) 15%, transparent)', 'color' => 'var(--zayne-color-warning)'],
                'info' => ['background' => 'color-mix(in oklch, var(--zayne-color-info) 15%, transparent)', 'color' => 'var(--zayne-color-info)'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'outline' => [
                'primary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)', 'border' => '1px solid var(--zayne-color-primary)'],
                'secondary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-secondary)', 'border' => '1px solid var(--zayne-color-secondary)'],
                'danger' => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)', 'border' => '1px solid var(--zayne-color-danger)'],
                'success' => ['background' => 'transparent', 'color' => 'var(--zayne-color-success)', 'border' => '1px solid var(--zayne-color-success)'],
                'warning' => ['background' => 'transparent', 'color' => 'var(--zayne-color-warning)', 'border' => '1px solid var(--zayne-color-warning)'],
                'info' => ['background' => 'transparent', 'color' => 'var(--zayne-color-info)', 'border' => '1px solid var(--zayne-color-info)'],
                'base' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'border' => '1px solid var(--zayne-color-base-border)'],
            ],
            'ghost' => [
                'primary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)'],
                'danger' => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)'],
                'base' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'link' => [
                'primary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)', 'text-decoration' => 'underline'],
                'danger' => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)', 'text-decoration' => 'underline'],
                'base' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'text-decoration' => 'underline'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['solid']['primary'];

        $base = [
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'gap' => $this->gap,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ];

        $this->style = Zayne::styleString(array_merge($base, $resolved));
    }

    public function render(): View
    {
        return view('zayne::button');
    }
}

========== ./src/Components/Card.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Card extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'solid',
        public string $color = 'base',
        public string $padding = '1.25rem',
        public string $radius = 'var(--zayne-radius-box)',
        public ?string $shadow = null,
        public ?string $margin = null,
        public string $border = 'var(--zayne-border-box)',
        public string $bordercolor = 'var(--zayne-color-base-border)'
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'solid' => [
                'base' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-base-content)'],
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 8%, var(--zayne-color-base-100))', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)'],
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 12%, var(--zayne-color-base-100))', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'outline' => [
                'base' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'border' => '1px solid var(--zayne-color-base-border)'],
                'primary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'border' => '1px solid var(--zayne-color-primary)'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['solid']['base'];

        $this->style = Zayne::styleString(array_merge([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::card');
    }
}

========== ./src/Components/Checkbox.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Checkbox extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'outline',
        public string $color = 'primary',
        public bool $checked = false,
        public ?string $padding = null,
        public string $radius = 'var(--zayne-radius-selector)',
        public ?string $shadow = null,
        public ?string $margin = null,
        public string $border = 'var(--zayne-border-selector)',
        public string $bordercolor = 'var(--zayne-color-base-border)'
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'outline' => [
                'primary' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-primary)', 'border-color' => 'var(--zayne-color-primary)'],
                'base' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'var(--zayne-color-base-border)'],
            ],
            'soft' => [
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 20%, transparent)', 'color' => 'var(--zayne-color-primary)', 'border-color' => 'transparent'],
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'transparent'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['outline']['primary'];

        if ($this->checked) {
            $resolved['background'] = $this->color === 'base' ? 'var(--zayne-color-base-content)' : 'var(--zayne-color-primary)';
            $resolved['color'] = $this->color === 'base' ? 'var(--zayne-color-base-100)' : 'var(--zayne-color-primary-content)';
        }

        $this->style = Zayne::styleString(array_merge([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::checkbox');
    }
}

========== ./src/Components/Drawer.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Drawer extends Component
{
    public string $style = '';

    public function __construct(
        public string $width = '22rem',
        public string $padding = '1.5rem',
        public string $shadow = 'var(--zayne-shadow)',
        public string $background = 'var(--zayne-color-base-100)',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $this->style = Zayne::styleString([
            'width' => $this->width,
            'padding' => $this->padding,
            'box-shadow' => $this->shadow,
            'background' => $this->background,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
            'border-style' => 'solid',
        ]);
    }

    public function render(): View
    {
        return view('zayne::drawer');
    }
}

========== ./src/Components/Dropdown.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Dropdown extends Component
{
    public string $style = '';

    public function __construct(
        public string $padding = '0.5rem',
        public string $radius = 'var(--zayne-radius-box)',
        public string $shadow = 'var(--zayne-shadow)',
        public string $background = 'var(--zayne-color-base-100)',
        public string $minwidth = '12rem',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $this->style = Zayne::styleString([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'background' => $this->background,
            'min-width' => $this->minwidth,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
            'border-style' => 'solid',
        ]);
    }

    public function render(): View
    {
        return view('zayne::dropdown');
    }
}

========== ./src/Components/Header/Avatar.php ==========
<?php

namespace Zayne\UI\Components\Header;

use Illuminate\View\Component;
use Illuminate\View\View;

class Avatar extends Component
{
    public function __construct(
        public ?string $src = null,
        public string $alt = 'Avatar',
        public ?string $label = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::header.avatar');
    }
}

========== ./src/Components/Header/Brand.php ==========
<?php

namespace Zayne\UI\Components\Header;

use Illuminate\View\Component;
use Illuminate\View\View;

class Brand extends Component
{
    public function __construct(
        public string $name = 'Zayne UI',
        public ?string $href = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::header.brand');
    }
}

========== ./src/Components/Header/Nav.php ==========
<?php

namespace Zayne\UI\Components\Header;

use Illuminate\View\Component;
use Illuminate\View\View;

class Nav extends Component
{
    public function __construct(
        public string $gap = '0.5rem'
    ) {
    }

    public function render(): View
    {
        return view('zayne::header.nav');
    }
}

========== ./src/Components/Input.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Input extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'outline',
        public string $color = 'base',
        public string $type = 'text',
        public ?string $value = null,
        public ?string $placeholder = null,
        public string $padding = '0 0.875rem',
        public string $radius = 'var(--zayne-radius-field)',
        public ?string $shadow = null,
        public ?string $margin = null,
        public string $border = 'var(--zayne-border-field)',
        public string $bordercolor = 'var(--zayne-color-base-border)'
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'outline' => [
                'base' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-base-content)'],
                'primary' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'var(--zayne-color-primary)'],
            ],
            'soft' => [
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'transparent'],
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 10%, var(--zayne-color-base-100))', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'transparent'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['outline']['base'];

        $this->style = Zayne::styleString(array_merge([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::input');
    }
}

========== ./src/Components/Layout/Header.php ==========
<?php

namespace Zayne\UI\Components\Layout;

use Illuminate\View\Component;
use Illuminate\View\View;

class Header extends Component
{
    public function __construct(
        public string $background = 'var(--zayne-custom-header)',
        public string $shadow = 'var(--zayne-shadow)',
        public string $padding = 'var(--zayne-padding-header)',
        public string $gap = 'var(--zayne-gap-header)',
        public ?string $border = null,
        public ?string $bordertop = null,
        public ?string $borderbottom = null,
        public ?string $borderleft = null,
        public ?string $borderright = null,
        public ?string $bordercolor = null,
        public ?string $margin = null,
        public ?string $margintop = null,
        public ?string $marginbottom = null,
        public ?string $marginleft = null,
        public ?string $marginright = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::layout.header');
    }
}

========== ./src/Components/Layout/Main.php ==========
<?php

namespace Zayne\UI\Components\Layout;

use Illuminate\View\Component;
use Illuminate\View\View;

class Main extends Component
{
    public function __construct(
        public string $background = 'var(--zayne-color-base-100)',
        public ?string $padding = null,
        public ?string $margin = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::layout.main');
    }
}

========== ./src/Components/Layout.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;

class Layout extends Component
{
    public function __construct()
    {
    }

    public function render(): View
    {
        return view('zayne::layout');
    }
}

========== ./src/Components/Layout/Sidebar.php ==========
<?php

namespace Zayne\UI\Components\Layout;

use Illuminate\View\Component;
use Illuminate\View\View;

class Sidebar extends Component
{
    public function __construct(
        public string $background = 'var(--zayne-custom-sidebar)',
        public string $padding = '1rem',
        public string $gap = '0.75rem',
        public ?string $shadow = null,
        public ?string $radius = null,
        public ?string $border = null,
        public ?string $bordertop = null,
        public ?string $borderbottom = null,
        public ?string $borderleft = null,
        public ?string $borderright = null,
        public ?string $bordercolor = null,
        public ?string $margin = null,
        public ?string $margintop = null,
        public ?string $marginbottom = null,
        public ?string $marginleft = null,
        public ?string $marginright = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::layout.sidebar');
    }
}

========== ./src/Components/Modal.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Modal extends Component
{
    public string $style = '';

    public function __construct(
        public string $width = 'min(90vw, 40rem)',
        public string $padding = '1.5rem',
        public string $radius = 'var(--zayne-radius-box)',
        public string $shadow = 'var(--zayne-shadow)',
        public string $background = 'var(--zayne-color-base-100)',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $this->style = Zayne::styleString([
            'width' => $this->width,
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'background' => $this->background,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
            'border-style' => 'solid',
        ]);
    }

    public function render(): View
    {
        return view('zayne::modal');
    }
}

========== ./src/Components/Popover.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Popover extends Component
{
    public string $style = '';

    public function __construct(
        public string $padding = '1rem',
        public string $radius = 'var(--zayne-radius-box)',
        public string $shadow = 'var(--zayne-shadow)',
        public string $background = 'var(--zayne-color-base-100)',
        public string $minwidth = '16rem',
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $this->style = Zayne::styleString([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'background' => $this->background,
            'min-width' => $this->minwidth,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
            'border-style' => 'solid',
        ]);
    }

    public function render(): View
    {
        return view('zayne::popover');
    }
}

========== ./src/Components/Progress.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Progress extends Component
{
    public string $style = '';

    public string $barStyle = '';

    public function __construct(
        public string $variant = 'solid',
        public string $color = 'primary',
        public int|string $value = 0,
        public string $height = '8px',
        public string $radius = '999px',
        public ?string $shadow = null,
        public ?string $margin = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'solid' => [
                'primary' => ['background' => 'var(--zayne-color-primary)'],
                'secondary' => ['background' => 'var(--zayne-color-secondary)'],
                'danger' => ['background' => 'var(--zayne-color-danger)'],
                'success' => ['background' => 'var(--zayne-color-success)'],
                'warning' => ['background' => 'var(--zayne-color-warning)'],
                'info' => ['background' => 'var(--zayne-color-info)'],
                'base' => ['background' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 70%, white)'],
                'secondary' => ['background' => 'color-mix(in oklch, var(--zayne-color-secondary) 70%, white)'],
                'danger' => ['background' => 'color-mix(in oklch, var(--zayne-color-danger) 70%, white)'],
                'success' => ['background' => 'color-mix(in oklch, var(--zayne-color-success) 70%, white)'],
                'warning' => ['background' => 'color-mix(in oklch, var(--zayne-color-warning) 70%, white)'],
                'info' => ['background' => 'color-mix(in oklch, var(--zayne-color-info) 70%, white)'],
                'base' => ['background' => 'var(--zayne-color-base-content-muted)'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['solid']['primary'];

        $this->style = Zayne::styleString([
            'height' => $this->height,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
        ]);

        $this->barStyle = Zayne::styleString(array_merge([
            'width' => max(0, min(100, (int) $this->value)) . '%',
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::progress');
    }
}

========== ./src/Components/Select.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Select extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'outline',
        public string $color = 'base',
        public string $padding = '0 0.875rem',
        public string $radius = 'var(--zayne-radius-field)',
        public ?string $shadow = null,
        public ?string $margin = null,
        public string $border = 'var(--zayne-border-field)',
        public string $bordercolor = 'var(--zayne-color-base-border)'
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'outline' => [
                'base' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-base-content)'],
                'primary' => ['background' => 'var(--zayne-color-base-100)', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'var(--zayne-color-primary)'],
            ],
            'soft' => [
                'base' => ['background' => 'var(--zayne-color-base-200)', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'transparent'],
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 10%, var(--zayne-color-base-100))', 'color' => 'var(--zayne-color-base-content)', 'border-color' => 'transparent'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['outline']['base'];

        $this->style = Zayne::styleString(array_merge([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::select');
    }
}

========== ./src/Components/Sidebar/Avatar.php ==========
<?php

namespace Zayne\UI\Components\Sidebar;

use Illuminate\View\Component;
use Illuminate\View\View;

class Avatar extends Component
{
    public function __construct(
        public ?string $src = null,
        public string $alt = 'Avatar',
        public ?string $label = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::sidebar.avatar');
    }
}

========== ./src/Components/Sidebar/Brand.php ==========
<?php

namespace Zayne\UI\Components\Sidebar;

use Illuminate\View\Component;
use Illuminate\View\View;

class Brand extends Component
{
    public function __construct(
        public string $name = 'Zayne UI',
        public ?string $href = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::sidebar.brand');
    }
}

========== ./src/Components/Sidebar/Label.php ==========
<?php

namespace Zayne\UI\Components\Sidebar;

use Illuminate\View\Component;
use Illuminate\View\View;

class Label extends Component
{
    public function __construct()
    {
    }

    public function render(): View
    {
        return view('zayne::sidebar.label');
    }
}

========== ./src/Components/Sidebar/Navitem.php ==========
<?php

namespace Zayne\UI\Components\Sidebar;

use Illuminate\View\Component;
use Illuminate\View\View;

class Navitem extends Component
{
    public function __construct(
        public ?string $href = null,
        public bool $active = false
    ) {
    }

    public function render(): View
    {
        return view('zayne::sidebar.navitem');
    }
}

========== ./src/Components/Sidebar/Navtreeitem.php ==========
<?php

namespace Zayne\UI\Components\Sidebar;

use Illuminate\View\Component;
use Illuminate\View\View;

class Navtreeitem extends Component
{
    public function __construct(
        public ?string $href = null,
        public bool $active = false
    ) {
    }

    public function render(): View
    {
        return view('zayne::sidebar.navtreeitem');
    }
}

========== ./src/Components/Sidebar/Navtree.php ==========
<?php

namespace Zayne\UI\Components\Sidebar;

use Illuminate\View\Component;
use Illuminate\View\View;

class Navtree extends Component
{
    public function __construct(
        public ?string $title = null
    ) {
    }

    public function render(): View
    {
        return view('zayne::sidebar.navtree');
    }
}

========== ./src/Components/Toggle.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Toggle extends Component
{
    public string $style = '';

    public function __construct(
        public string $variant = 'soft',
        public string $color = 'primary',
        public bool $checked = false,
        public string $padding = '0.125rem',
        public string $radius = '999px',
        public ?string $shadow = null,
        public ?string $margin = null,
        public ?string $border = null,
        public ?string $bordercolor = null
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $variantStyles = [
            'soft' => [
                'primary' => ['background' => 'color-mix(in oklch, var(--zayne-color-primary) 18%, var(--zayne-color-base-300))', 'color' => 'var(--zayne-color-primary)'],
                'success' => ['background' => 'color-mix(in oklch, var(--zayne-color-success) 18%, var(--zayne-color-base-300))', 'color' => 'var(--zayne-color-success)'],
                'base' => ['background' => 'var(--zayne-color-base-300)', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'solid' => [
                'primary' => ['background' => 'var(--zayne-color-primary)', 'color' => 'var(--zayne-color-primary-content)'],
                'success' => ['background' => 'var(--zayne-color-success)', 'color' => 'var(--zayne-color-success-content)'],
                'base' => ['background' => 'var(--zayne-color-base-content)', 'color' => 'var(--zayne-color-base-100)'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['soft']['primary'];

        if (! $this->checked) {
            $resolved['background'] = 'var(--zayne-color-base-300)';
            $resolved['color'] = 'var(--zayne-color-base-content-muted)';
        }

        $this->style = Zayne::styleString(array_merge([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'margin' => $this->margin,
            'border-width' => $this->border,
            'border-color' => $this->bordercolor,
        ], $resolved));
    }

    public function render(): View
    {
        return view('zayne::toggle');
    }
}

========== ./src/Components/Tooltip.php ==========
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Tooltip extends Component
{
    public string $style = '';

    public function __construct(
        public string $text = 'Tooltip',
        public string $padding = '0.5rem 0.75rem',
        public string $radius = 'var(--zayne-radius-selector)',
        public string $shadow = 'var(--zayne-shadow)',
        public string $background = 'var(--zayne-color-base-300)',
        public string $color = 'var(--zayne-color-base-content)'
    ) {
        $this->buildStyle();
    }

    protected function buildStyle(): void
    {
        $this->style = Zayne::styleString([
            'padding' => $this->padding,
            'border-radius' => $this->radius,
            'box-shadow' => $this->shadow,
            'background' => $this->background,
            'color' => $this->color,
        ]);
    }

    public function render(): View
    {
        return view('zayne::tooltip');
    }
}

========== ./src/Console/InstallCommand.php ==========
<?php

namespace Zayne\UI\Console;

use Illuminate\Console\Command;
use Illuminate\Filesystem\Filesystem;
use Symfony\Component\Console\Attribute\AsCommand;

#[AsCommand(name: 'zayne:install')]
class InstallCommand extends Command
{
    protected $signature = 'zayne:install';

    protected $description = 'Wire ZayneUI directives into your Laravel app.';

    protected Filesystem $files;

    public function __construct(Filesystem $files)
    {
        parent::__construct();

        $this->files = $files;
    }

    public function handle(): int
    {
        $this->components->info('Setting up ZayneUI...');
        $this->newLine();

        $this->ensureAlpineJs();
        $this->registerServiceProvider();

        $this->newLine();
        $this->components->info('ZayneUI is ready.');
        $this->newLine();
        $this->line('  <fg=gray>Add to your layout:</>');
        $this->line('  <fg=cyan>@zayneStyles</> inside <fg=cyan><head></>');
        $this->line('  <fg=cyan>@zayneScripts</> before <fg=cyan></body></>');
        $this->line('  <fg=cyan>@zayneAppearance</> inside <fg=cyan><head></> (optional, for theme persistence)');
        $this->newLine();
        $this->line('  <fg=gray>Set theme on <html>:</> <fg=cyan>class="light"</> | <fg=cyan>class="dark"</> | <fg=cyan>class="abyss"</>');
        $this->newLine();

        return self::SUCCESS;
    }

    protected function ensureAlpineJs(): void
    {
        $packageJsonPath = base_path('package.json');

        if (! $this->files->exists($packageJsonPath)) {
            $this->components->warn('package.json not found. Install Alpine.js manually: npm install alpinejs');

            return;
        }

        $decoded = json_decode($this->files->get($packageJsonPath), true);
        $all = array_merge($decoded['dependencies'] ?? [], $decoded['devDependencies'] ?? []);

        if (array_key_exists('alpinejs', $all)) {
            $this->components->twoColumnDetail('<fg=green>Found</> Alpine.js', '<fg=gray>alpinejs</>');

            return;
        }

        $this->components->warn('Alpine.js not found in package.json.');
        $this->line('  Run: <fg=cyan>npm install alpinejs</>');
    }

    protected function registerServiceProvider(): void
    {
        $bootstrapPath = base_path('bootstrap/providers.php');

        if (! $this->files->exists($bootstrapPath)) {
            $this->components->warn('bootstrap/providers.php not found. ZayneUI auto-discovers via composer.json.');

            return;
        }

        $this->components->twoColumnDetail('<fg=green>Auto-discovered</> ZayneServiceProvider', '<fg=gray>via composer.json</>');
    }
}

========== ./src/Console/PublishCommand.php ==========
<?php

namespace Zayne\UI\Console;

use Illuminate\Console\Command;
use Illuminate\Filesystem\Filesystem;
use Symfony\Component\Console\Attribute\AsCommand;

#[AsCommand(name: 'zayne:publish')]
class PublishCommand extends Command
{
    protected $signature = 'zayne:publish
                            {components?* : Component name(s) to publish}
                            {--all : Publish all components}
                            {--force : Overwrite existing files}';

    protected $description = 'Publish Zayne UI components for customization.';

    protected Filesystem $files;

    protected array $publishableComponents = [
        'button', 'badge', 'alert', 'avatar', 'card', 'progress',
        'modal', 'drawer', 'dropdown', 'tooltip', 'popover',
        'input', 'select', 'checkbox', 'toggle',
        'layout/header', 'layout/main', 'layout/sidebar',
        'header/brand', 'header/avatar', 'header/nav',
        'sidebar/brand', 'sidebar/avatar', 'sidebar/label',
        'sidebar/navitem', 'sidebar/navtree', 'sidebar/navtreeitem',
    ];

    public function __construct(Filesystem $files)
    {
        parent::__construct();

        $this->files = $files;
    }

    public function handle(): int
    {
        $targets = $this->option('all')
            ? $this->publishableComponents
            : ($this->argument('components') ?: $this->askWhichComponent());

        foreach ($targets as $component) {
            $this->publishComponent($component);
        }

        return self::SUCCESS;
    }

    protected function askWhichComponent(): array
    {
        $choice = $this->choice('Which component would you like to publish?', $this->publishableComponents);

        return [$choice];
    }

    protected function publishComponent(string $component): void
    {
        $force = $this->option('force');

        $bladeSrc = __DIR__ . '/../../stubs/resources/views/components/zayne/' . $component . '.blade.php';
        $bladeDest = resource_path('views/components/zayne/' . $component . '.blade.php');

        if ($this->files->exists($bladeSrc)) {
            if ($this->files->exists($bladeDest) && ! $force) {
                $this->components->twoColumnDetail("<fg=yellow>Skipped</> {$component}.blade.php", '<fg=gray>use --force to overwrite</>');
            } else {
                $this->files->ensureDirectoryExists(dirname($bladeDest));
                $this->files->copy($bladeSrc, $bladeDest);
                $this->components->twoColumnDetail("<fg=green>Published</> {$component}.blade.php", '<fg=gray>resources/views/components/zayne/</>');
            }
        }

        $className = $this->componentToClassName($component);
        $phpSrc = __DIR__ . '/../Components/' . $className . '.php';
        $phpDest = app_path('View/Components/Zayne/' . $className . '.php');

        if ($this->files->exists($phpSrc)) {
            if ($this->files->exists($phpDest) && ! $force) {
                $this->components->twoColumnDetail("<fg=yellow>Skipped</> {$className}.php", '<fg=gray>use --force to overwrite</>');
            } else {
                $this->files->ensureDirectoryExists(dirname($phpDest));
                $contents = str_replace(
                    'namespace Zayne\\UI\\Components',
                    'namespace App\\View\\Components\\Zayne',
                    $this->files->get($phpSrc)
                );
                $this->files->put($phpDest, $contents);
                $this->components->twoColumnDetail("<fg=green>Published</> {$className}.php", '<fg=gray>app/View/Components/Zayne/</>');
            }
        }
    }

    protected function componentToClassName(string $component): string
    {
        return collect(explode('/', $component))
            ->map(fn ($part) => str($part)->studly())
            ->implode('/');
    }
}

========== ./src/ZayneAssetManager.php ==========
<?php

namespace Zayne\UI;

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

class ZayneAssetManager
{
    public static function boot(): void
    {
        $instance = new static();
        $instance->registerRoutes();
    }

    protected function registerRoutes(): void
    {
        Route::get('/zayne/zayne.css', [static::class, 'css']);
        Route::get('/zayne/zayne.js', [static::class, 'js']);
    }

    public function css(): mixed
    {
        return $this->pretendResponseIsFile(__DIR__ . '/../stubs/resources/css/zayne.css', 'text/css');
    }

    public function js(): mixed
    {
        return $this->pretendResponseIsFile(__DIR__ . '/../stubs/resources/js/zayne.js', 'text/javascript');
    }

    public static function renderStyles(): string
    {
        $version = filemtime(__DIR__ . '/../stubs/resources/css/zayne.css');

        return '<link rel="stylesheet" href="' . url('/zayne/zayne.css?v=' . $version) . '">';
    }

    public static function renderScripts(): string
    {
        $version = filemtime(__DIR__ . '/../stubs/resources/js/zayne.js');

        return '<script src="' . url('/zayne/zayne.js?v=' . $version) . '" defer></script>';
    }

    public static function renderAppearance(): string
    {
        return <<<HTML
<script>
    (function() {
        var saved = localStorage.getItem('zayne.theme') || 'light';
        document.documentElement.className = saved;
    })();
</script>
HTML;
    }

    protected function pretendResponseIsFile(string $file, string $contentType): mixed
    {
        $lastModified = filemtime($file);

        return $this->cachedFileResponse(
            $file,
            $contentType,
            $lastModified,
            fn ($headers) => response()->file($file, $headers)
        );
    }

    protected function cachedFileResponse(string $filename, string $contentType, int $lastModified, callable $downloadCallback): mixed
    {
        $expires = strtotime('+1 year');
        $cacheControl = 'public, max-age=31536000';

        if ($this->matchesCache($lastModified)) {
            return response('', 304, [
                'Expires' => $this->httpDate($expires),
                'Cache-Control' => $cacheControl,
            ]);
        }

        return $downloadCallback([
            'Content-Type' => $contentType,
            'Expires' => $this->httpDate($expires),
            'Cache-Control' => $cacheControl,
            'Last-Modified' => $this->httpDate($lastModified),
        ]);
    }

    protected function matchesCache(int $lastModified): bool
    {
        $ifModifiedSince = app(Request::class)->header('if-modified-since');

        return $ifModifiedSince !== null && @strtotime($ifModifiedSince) === $lastModified;
    }

    protected function httpDate(int $timestamp): string
    {
        return sprintf('%s GMT', gmdate('D, d M Y H:i:s', $timestamp));
    }
}

========== ./src/ZayneManager.php ==========
<?php

namespace Zayne\UI;

use Illuminate\Support\Str;
use Illuminate\View\ComponentAttributeBag;

class ZayneManager
{
    public function styleString(array $styles): string
    {
        return collect($styles)
            ->filter(fn ($value) => $value !== null && $value !== '')
            ->map(fn ($value, $key) => "{$key}: {$value}")
            ->implode('; ');
    }

    public function splitAttributes(ComponentAttributeBag $attributes, array $by = ['class', 'style'], bool $strict = false): array
    {
        return [
            $strict ? $attributes->only($by) : $attributes->whereStartsWith($by),
            $strict ? $attributes->except($by) : $attributes->whereDoesntStartWith($by),
        ];
    }

    public function forwardedAttributes(ComponentAttributeBag $attributes, array $propKeys): array
    {
        $props = [];
        $unescape = fn ($value) => is_string($value) ? htmlspecialchars_decode($value, ENT_QUOTES) : $value;

        foreach ($propKeys as $key) {
            if (isset($attributes[$key])) {
                $props[$key] = $unescape($attributes[$key]);
            } elseif (isset($attributes[Str::kebab($key)])) {
                $props[$key] = $unescape($attributes[Str::kebab($key)]);
            }
        }

        return $props;
    }

    public function attributesAfter(string $prefix, ComponentAttributeBag $attributes, array $default = []): ComponentAttributeBag
    {
        $newAttributes = new ComponentAttributeBag($default);
        $keysToRemove = [];

        foreach ($attributes->getAttributes() as $key => $value) {
            if (str_starts_with($key, $prefix)) {
                $newAttributes[substr($key, strlen($prefix))] = $value;
                $keysToRemove[] = $key;
            }
        }

        foreach ($keysToRemove as $key) {
            unset($attributes[$key]);
        }

        return $newAttributes;
    }

    public function renderStyles(): string
    {
        return ZayneAssetManager::renderStyles();
    }

    public function renderScripts(): string
    {
        return ZayneAssetManager::renderScripts();
    }

    public function renderAppearance(): string
    {
        return ZayneAssetManager::renderAppearance();
    }
}

========== ./src/Zayne.php ==========
<?php

namespace Zayne\UI;

use Illuminate\Support\Facades\Facade;

/**
 * @see \Zayne\UI\ZayneManager
 */
class Zayne extends Facade
{
    public static function getFacadeAccessor(): string
    {
        return 'zayne';
    }
}

========== ./src/ZayneServiceProvider.php ==========
<?php

namespace Zayne\UI;

use Illuminate\Foundation\AliasLoader;
use Illuminate\Support\Facades\Blade;
use Illuminate\Support\ServiceProvider;
use Illuminate\View\ComponentAttributeBag;

class ZayneServiceProvider extends ServiceProvider
{
    public function register(): void
    {
        $this->app->singleton(ZayneManager::class);
        $this->app->alias(ZayneManager::class, 'zayne');

        $loader = AliasLoader::getInstance();
        $loader->alias('Zayne', Zayne::class);
    }

    public function boot(): void
    {
        $this->bootComponents();
        $this->bootTagCompiler();
        $this->bootAssetManager();
        $this->bootDirectives();
        $this->bootMacros();
        $this->bootCommands();
    }

    protected function bootComponents(): void
    {
        Blade::componentNamespace('Zayne\\UI\\Components', 'zayne');

        $packageViewPath = __DIR__ . '/../stubs/resources/views/components/zayne';

        $this->loadViewsFrom($packageViewPath, 'zayne');

        if (function_exists('resource_path') && file_exists(resource_path('views/components/zayne'))) {
            Blade::anonymousComponentPath(resource_path('views/components/zayne'), 'zayne');
        }

        Blade::anonymousComponentPath($packageViewPath, 'zayne');
    }

    protected function bootTagCompiler(): void
    {
        $compiler = new ZayneTagCompiler(
            app('blade.compiler')->getClassComponentAliases(),
            app('blade.compiler')->getClassComponentNamespaces(),
            app('blade.compiler')
        );

        app()->bind('zayne.compiler', fn () => $compiler);

        app('blade.compiler')->precompiler(function ($value) use ($compiler) {
            return $compiler->compile($value);
        });
    }

    protected function bootAssetManager(): void
    {
        ZayneAssetManager::boot();
    }

    protected function bootDirectives(): void
    {
        Blade::directive('zayneStyles', fn () => "<?php echo app('zayne')->renderStyles(); ?>");
        Blade::directive('zayneScripts', fn () => "<?php echo app('zayne')->renderScripts(); ?>");
        Blade::directive('zayneAppearance', fn () => "<?php echo app('zayne')->renderAppearance(); ?>");
    }

    protected function bootMacros(): void
    {
        ComponentAttributeBag::macro('pluck', function ($key, $default = null) {
            $result = $this->get($key);
            unset($this->attributes[$key]);

            return $result ?? $default;
        });
    }

    protected function bootCommands(): void
    {
        if (! $this->app->runningInConsole()) {
            return;
        }

        $this->commands([
            Console\InstallCommand::class,
            Console\PublishCommand::class,
        ]);
    }
}

========== ./src/ZayneTagCompiler.php ==========
<?php

namespace Zayne\UI;

use Illuminate\View\Compilers\ComponentTagCompiler;

class ZayneTagCompiler extends ComponentTagCompiler
{
    protected function compileOpeningTags(string $value): string
    {
        $pattern = "/
            <
                \s*
                zayne[\:][\w\-\:\.]*
                (?<attributes>
                    (?:
                        \s+
                        (?:
                            (?:
                                @(?:class)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                @(?:style)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                \{\{\s*\\\$attributes(?:[^}]+?)?\s*\}\}
                            )
                            |
                            (?:
                                [\w\-:.@%]+
                                (
                                    =
                                    (?:
                                        \\\"[^\\\"]*\\\"
                                        |
                                        '[^']*'
                                        |
                                        [^'\\\"=<>]+
                                    )
                                )?
                            )
                        )
                    )*
                    \s*
                )
                (?<![\/=\-])
            >
        /x";

        return preg_replace_callback($pattern, function (array $matches) {
            $this->boundAttributes = [];
            $component = 'zayne::' . $this->extractComponentName($matches[0]);
            $attributes = $this->getAttributesFromAttributeString($matches['attributes']);

            return $this->componentString($component, $attributes);
        }, $value);
    }

    protected function compileSelfClosingTags(string $value): string
    {
        $pattern = "/
            <
                \s*
                zayne[\:]([\w\-\:\.]*)
                \s*
                (?<attributes>
                    (?:
                        \s+
                        (?:
                            (?:
                                @(?:class)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                @(?:style)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                \{\{\s*\\\$attributes(?:[^}]+?)?\s*\}\}
                            )
                            |
                            (?:
                                [\w\-:.@%]+
                                (
                                    =
                                    (?:
                                        \\\"[^\\\"]*\\\"
                                        |
                                        '[^']*'
                                        |
                                        [^'\\\"=<>]+
                                    )
                                )?
                            )
                        )
                    )*
                    \s*
                )
            \/>
        /x";

        return preg_replace_callback($pattern, function (array $matches) {
            $this->boundAttributes = [];
            $attributes = $this->getAttributesFromAttributeString($matches['attributes']);

            if (isset($attributes['slot'])) {
                $slot = $attributes['slot'];
                unset($attributes['slot']);

                return '@slot(' . $slot . ') '
                    . $this->componentString('zayne::' . $matches[1], $attributes)
                    . "\n@endComponentClass##END-COMPONENT-CLASS##"
                    . ' @endslot';
            }

            return $this->componentString('zayne::' . $matches[1], $attributes)
                . "\n@endComponentClass##END-COMPONENT-CLASS##";
        }, $value);
    }

    protected function compileClosingTags(string $value): string
    {
        return preg_replace(
            "/<\/\s*zayne[\:][\w\-\:\.]*\s*>/",
            ' @endComponentClass##END-COMPONENT-CLASS##',
            $value
        );
    }

    protected function extractComponentName(string $tag): string
    {
        preg_match('/<\s*zayne[\:]([\w\-\:\.]*)/i', $tag, $matches);

        return $matches[1] ?? '';
    }
}

========== ./stubs/resources/css/zayne.css ==========
/* ============================================================
|  zayne.css - ZayneUI Design System
|  1. TOKENS
|  2. STRUCTURE
|  3. ANIMATIONSxc
============================================================ */

/* ============================================================
|  SECTION 1 - TOKENS
============================================================ */

:root {
    --zayne-radius-box: 10px;
    --zayne-radius-field: 10px;
    --zayne-radius-selector: 4px;
    --zayne-border-box: 1px;
    --zayne-border-field: 1px;
    --zayne-border-selector: 2px;
    --zayne-size-field: 40px;
    --zayne-size-selector: 20px;
    --zayne-shadow: 0 8px 10px rgba(0, 0, 0, 0.08);
    --zayne-gap-button: 0.5rem;
    --zayne-padding-header: 0 1rem;
    --zayne-gap-header: 0;
}

.light {
    --zayne-color-base-100: oklch(100% 0.00011 271.152);
    --zayne-color-base-200: oklch(97.015% 0.00011 271.152);
    --zayne-color-base-300: oklch(90.67% 0.0001 271.152);
    --zayne-color-base-content: oklch(18% 0.005 271);
    --zayne-color-base-content-muted: oklch(59.31% 0.00007 271.152);
    --zayne-color-base-border: oklch(82% 0.004 271);
    --zayne-color-base-hover: oklch(91% 0.003 271);
    --zayne-color-primary: oklch(98% 0.002 271);
    --zayne-color-primary-content: oklch(28% 0.01 271);
    --zayne-color-secondary: oklch(85% 0.199 91.936);
    --zayne-color-secondary-content: oklch(25% 0.05 91);
    --zayne-color-accent: oklch(87% 0.169 91.605);
    --zayne-color-accent-content: oklch(38% 0.063 188.416);
    --zayne-color-danger: oklch(55% 0.237 25.331);
    --zayne-color-danger-content: oklch(97% 0.01 25);
    --zayne-color-success: oklch(48% 0.15 149.579);
    --zayne-color-success-content: oklch(97% 0.01 149);
    --zayne-color-warning: oklch(81.79% 0.17046 77.947);
    --zayne-color-warning-content: oklch(15% 0.05 84);
    --zayne-color-info: oklch(52% 0.14 232.661);
    --zayne-color-info-content: oklch(97% 0.01 232);
    --zayne-custom-header: oklch(100% 0.00011 271.152);
    --zayne-custom-sidebar: #ffffff;
    --zayne-custom-sidebar-content: #222222;
    --zayne-custom-sidebar-item-bg: #24242409;
    --zayne-custom-sidebar-item-bg-hover: #ffbf00;
    --zayne-custom-sidebar-item-bg-active: rgb(6, 27, 57);
    --zayne-custom-sidebar-item-content: #ffffff;
    --zayne-custom-sidebar-item-content-hover: #0f0f0f;
    --zayne-custom-sidebar-item-content-active: #ffffff;
    --zayne-shadow: 0 8px 10px rgba(0, 0, 0, 0.08);
    --zayne-custom-layout-shadow: 0 8px 10px rgba(0, 0, 0, 0.08);
}

.dark {
    --zayne-color-base-100: oklch(18% 0.005 271);
    --zayne-color-base-200: oklch(22% 0.005 271);
    --zayne-color-base-300: oklch(27% 0.005 271);
    --zayne-color-base-content: oklch(88% 0.005 271);
    --zayne-color-base-content-muted: oklch(55% 0.005 271);
    --zayne-color-base-border: oklch(32% 0.005 271);
    --zayne-color-base-hover: oklch(30% 0.005 271);
    --zayne-color-primary: oklch(14% 0.005 271);
    --zayne-color-primary-content: oklch(92% 0.005 271);
    --zayne-color-secondary: oklch(85% 0.199 91.936);
    --zayne-color-secondary-content: oklch(25% 0.05 91);
    --zayne-color-danger: oklch(63% 0.237 25.331);
    --zayne-color-danger-content: oklch(96% 0.01 25);
    --zayne-color-success: oklch(72% 0.219 149.579);
    --zayne-color-success-content: oklch(96% 0.01 149);
    --zayne-color-warning: oklch(82% 0.189 84.429);
    --zayne-color-warning-content: oklch(25% 0.05 84);
    --zayne-color-info: oklch(74% 0.16 232.661);
    --zayne-color-info-content: oklch(96% 0.01 232);
    --zayne-custom-header: oklch(22% 0.005 271);
    --zayne-custom-sidebar: oklch(15% 0.005 271);
    --zayne-custom-sidebar-content: oklch(88% 0.005 271);
    --zayne-custom-sidebar-item-bg: rgba(255, 255, 255, 0.04);
    --zayne-custom-sidebar-item-bg-hover: rgba(255, 255, 255, 0.08);
    --zayne-custom-sidebar-item-bg-active: rgba(255, 255, 255, 0.12);
    --zayne-custom-sidebar-item-content: oklch(70% 0.005 271);
    --zayne-custom-sidebar-item-content-hover: oklch(95% 0.005 271);
    --zayne-custom-sidebar-item-content-active: #ffffff;
    --zayne-shadow: 0 8px 10px rgba(0, 0, 0, 0.4);
    --zayne-custom-layout-shadow: 0 8px 10px rgba(0, 0, 0, 0.08);
}

.abyss {
    --zayne-color-base-100: oklch(20% 0.08 209);
    --zayne-color-base-200: oklch(15% 0.08 209);
    --zayne-color-base-300: oklch(10% 0.08 209);
    --zayne-color-base-content: oklch(99.226% 0.00608 75.126);
    --zayne-color-base-content-muted: oklch(60.409% 0.0418 73.346);
    --zayne-color-base-border: oklch(30% 0.08 209);
    --zayne-color-base-hover: oklch(25% 0.08 209);
    --zayne-color-primary: oklch(50% 0.2653 125);
    --zayne-color-primary-content: oklch(92% 0.2653 125);
    --zayne-color-secondary: oklch(83.27% 0.0764 298.3);
    --zayne-color-secondary-content: oklch(43.27% 0.0764 298.3);
    --zayne-color-danger: oklch(65% 0.1985 24.22);
    --zayne-color-danger-content: oklch(27% 0.1985 24.22);
    --zayne-color-success: oklch(79% 0.209 151.711);
    --zayne-color-success-content: oklch(26% 0.065 152.934);
    --zayne-color-warning: oklch(84.8% 0.1962 84.62);
    --zayne-color-warning-content: oklch(44.8% 0.1962 84.62);
    --zayne-color-info: oklch(74% 0.16 232.661);
    --zayne-color-info-content: oklch(29% 0.066 243.157);
    --zayne-custom-header: oklch(15% 0.08 209);
    --zayne-custom-sidebar: oklch(10% 0.08 209);
    --zayne-custom-sidebar-content: oklch(99.226% 0.00608 75.126);
    --zayne-custom-sidebar-item-bg: rgba(255, 255, 255, 0.04);
    --zayne-custom-sidebar-item-bg-hover: rgba(255, 255, 255, 0.1);
    --zayne-custom-sidebar-item-bg-active: rgba(255, 255, 255, 0.15);
    --zayne-custom-sidebar-item-content: oklch(99.226% 0.00608 75.126);
    --zayne-custom-sidebar-item-content-hover: #ffffff;
    --zayne-custom-sidebar-item-content-active: #ffffff;
    --zayne-radius-selector: 2rem;
    --zayne-radius-field: 0.25rem;
    --zayne-radius-box: 0.5rem;
    --zayne-shadow: 0 8px 10px rgba(0, 0, 0, 0.4);
    --zayne-custom-layout-shadow: 0 8px 10px rgba(0, 0, 0, 0.4);
}

/* ============================================================
|  SECTION 2 - STRUCTURE
============================================================ */

:root {
    --ease-out-smooth: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-in-smooth: cubic-bezier(0.4, 0, 1, 1);
    --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
}

html, body {
    margin: 0;
    min-height: 100%;
    background: var(--zayne-color-base-100);
    color: var(--zayne-color-base-content);
    font-family: "Instrument Sans", "Segoe UI", sans-serif;
}

[x-cloak] {
    display: none !important;
}

.zayne-layout {
    display: grid;
    grid-template-columns: auto 1fr;
    grid-template-rows: 1fr;
    min-height: 100dvh;
    grid-template-areas: "sidebar main";
}

.zayne-sidebar {
    grid-area: sidebar;
    width: 260px;
    transition: width 260ms var(--ease-out-smooth);
    overflow: hidden;
    background: var(--zayne-custom-sidebar);
    color: var(--zayne-custom-sidebar-content);
}

.zayne-sidebar.is-collapsed {
    width: 64px;
}

.zayne-main-wrapper {
    grid-area: main;
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
    overflow: hidden;
}

.zayne-main {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    background: var(--zayne-color-base-100);
}

.zayne-layout-sidebar-shell {
    display: flex;
    flex-direction: column;
    min-height: 100%;
    box-sizing: border-box;
}

.zayne-layout-sidebar-body {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    flex: 1;
    min-height: 0;
}

.zayne-header {
    display: flex;
    flex-direction: row;
    align-items: center;
    min-height: var(--zayne-size-field);
    background: var(--zayne-custom-header);
    border-style: solid;
    box-sizing: border-box;
}

.zayne-header-left,
.zayne-header-right {
    flex-shrink: 0;
    display: flex;
    align-items: center;
}

.zayne-header-center {
    flex: 1;
    display: flex;
    align-items: center;
    min-width: 0;
}

.zayne-header-right {
    margin-left: auto;
}

.zayne-brand,
.zayne-sidebar-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    color: inherit;
    text-decoration: none;
}

.zayne-header-nav {
    display: inline-flex;
    align-items: center;
}

.zayne-header-avatar,
.zayne-sidebar-avatar {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
}

.zayne-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 500;
    cursor: pointer;
    border: none;
    text-decoration: none;
    flex-shrink: 0;
    transition: filter 150ms ease, transform 100ms ease, box-shadow 150ms ease;
    white-space: nowrap;
    border-style: solid;
    border-width: 0;
    box-sizing: border-box;
}

.zayne-button:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 2px;
}

.zayne-button:not([disabled]):hover {
    filter: brightness(1.08);
}

.zayne-button:not([disabled]):active {
    transform: translateY(1px);
    filter: brightness(0.95);
}

.zayne-button[disabled] {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
}

.zayne-button--xs { height: 1.5rem; font-size: 0.75rem; padding: 0 0.5rem; border-radius: var(--zayne-radius-selector); }
.zayne-button--sm { height: 2rem; font-size: 0.875rem; padding: 0 0.75rem; border-radius: var(--zayne-radius-field); }
.zayne-button--md { height: 2.5rem; font-size: 0.875rem; padding: 0 1rem; border-radius: var(--zayne-radius-field); }
.zayne-button--lg { height: 3rem; font-size: 1rem; padding: 0 1.25rem; border-radius: var(--zayne-radius-field); }
.zayne-button--xl { height: 3.5rem; font-size: 1.125rem; padding: 0 1.5rem; border-radius: var(--zayne-radius-box); }

.zayne-button-icon,
.zayne-button-label,
.zayne-button-trailing {
    display: inline-flex;
    align-items: center;
}

.zayne-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-weight: 500;
    white-space: nowrap;
    border-style: solid;
    border-width: 0;
    box-sizing: border-box;
}

.zayne-badge--sm { height: 1.25rem; font-size: 0.7rem; padding: 0 0.4rem; border-radius: var(--zayne-radius-selector); }
.zayne-badge--md { height: 1.5rem; font-size: 0.75rem; padding: 0 0.5rem; border-radius: var(--zayne-radius-selector); }
.zayne-badge--lg { height: 1.75rem; font-size: 0.8rem; padding: 0 0.625rem; border-radius: var(--zayne-radius-selector); }

.zayne-alert {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    border-style: solid;
    box-sizing: border-box;
}

.zayne-alert-content {
    flex: 1;
    min-width: 0;
}

.zayne-icon-wrapper {
    width: 38px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.zayne-backdrop {
    position: fixed;
    inset: 0;
    z-index: 40;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
}

.zayne-modal-root,
.zayne-drawer-root,
.zayne-dropdown-root,
.zayne-tooltip-root,
.zayne-popover-root {
    position: relative;
}

.zayne-modal-panel {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 50;
    background: var(--zayne-color-base-100);
    border-radius: var(--zayne-radius-box);
    box-shadow: var(--zayne-shadow);
    min-width: 320px;
    max-width: 90vw;
    max-height: 90dvh;
    overflow-y: auto;
    box-sizing: border-box;
}

.zayne-drawer-panel {
    position: fixed;
    top: 0;
    left: 0;
    height: 100%;
    z-index: 50;
    background: var(--zayne-color-base-100);
    box-shadow: var(--zayne-shadow);
    overflow-y: auto;
    box-sizing: border-box;
}

.zayne-dropdown-panel,
.zayne-tooltip-panel,
.zayne-popover-panel {
    position: absolute;
    top: calc(100% + 0.5rem);
    left: 0;
    z-index: 30;
    box-sizing: border-box;
}

.zayne-card {
    background: var(--zayne-color-base-100);
    border-radius: var(--zayne-radius-box);
    border: var(--zayne-border-box) solid var(--zayne-color-base-border);
    box-sizing: border-box;
}

.zayne-card-header,
.zayne-card-footer {
    margin-bottom: 1rem;
}

.zayne-card-footer {
    margin-bottom: 0;
    margin-top: 1rem;
}

.zayne-input {
    display: flex;
    align-items: center;
    width: 100%;
    height: var(--zayne-size-field);
    border: var(--zayne-border-field) solid var(--zayne-color-base-border);
    border-radius: var(--zayne-radius-field);
    background: var(--zayne-color-base-100);
    color: var(--zayne-color-base-content);
    transition: border-color 150ms ease, box-shadow 150ms ease;
    box-sizing: border-box;
}

.zayne-input:focus,
.zayne-input:focus-within {
    border-color: var(--zayne-color-primary);
    outline: none;
}

.zayne-select {
    appearance: none;
}

.zayne-checkbox,
.zayne-toggle {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    cursor: pointer;
}

.zayne-checkbox-box {
    width: var(--zayne-size-selector);
    height: var(--zayne-size-selector);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-style: solid;
    box-sizing: border-box;
    position: relative;
    overflow: hidden;
}

.zayne-checkbox-box input,
.zayne-toggle-track input {
    position: absolute;
    inset: 0;
    opacity: 0;
    cursor: pointer;
}

.zayne-checkbox-box svg {
    width: 14px;
    height: 14px;
}

.zayne-toggle-track {
    width: 44px;
    height: 24px;
    display: inline-flex;
    align-items: center;
    position: relative;
    box-sizing: border-box;
}

.zayne-toggle-thumb {
    width: 18px;
    height: 18px;
    border-radius: 999px;
    background: currentColor;
    transform: translateX(0);
    transition: transform 180ms var(--ease-out-smooth);
}

.zayne-toggle-thumb.is-on {
    transform: translateX(20px);
}

.zayne-avatar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    flex-shrink: 0;
    border-style: solid;
    box-sizing: border-box;
}

.zayne-avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.zayne-avatar--sm { width: 2rem; height: 2rem; font-size: 0.75rem; }
.zayne-avatar--md { width: 2.5rem; height: 2.5rem; font-size: 0.875rem; }
.zayne-avatar--lg { width: 3rem; height: 3rem; font-size: 1rem; }
.zayne-avatar--xl { width: 4rem; height: 4rem; font-size: 1.25rem; }

.zayne-progress {
    width: 100%;
    height: 8px;
    border-radius: 999px;
    background: var(--zayne-color-base-300);
    overflow: hidden;
}

.zayne-progress-bar {
    height: 100%;
    border-radius: 999px;
    transition: width 300ms ease;
}

.zayne-sidebar-label {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--zayne-custom-sidebar-content);
    opacity: 0.5;
    transition: opacity 200ms;
}

.sidebar-label {
    white-space: nowrap;
    overflow: hidden;
}

.zayne-sidebar.is-collapsed .sidebar-label,
.zayne-sidebar.is-collapsed .zayne-sidebar-label {
    opacity: 0;
    width: 0;
    pointer-events: none;
}

.zayne-navtree {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.zayne-navtree-items {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
}

.zayne-navitem {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 0.75rem;
    border-radius: var(--zayne-radius-field);
    cursor: pointer;
    transition: background 150ms ease, color 150ms ease;
    text-decoration: none;
    color: var(--zayne-custom-sidebar-item-content);
    background: var(--zayne-custom-sidebar-item-bg);
    font-size: 0.875rem;
    font-weight: 500;
    border: none;
    width: 100%;
    box-sizing: border-box;
}

.zayne-navitem:hover {
    background: var(--zayne-custom-sidebar-item-bg-hover);
    color: var(--zayne-custom-sidebar-item-content-hover);
}

.zayne-navitem.active {
    background: var(--zayne-custom-sidebar-item-bg-active);
    color: var(--zayne-custom-sidebar-item-content-active);
}

.zayne-auth-shell {
    min-height: 100dvh;
    display: grid;
    place-items: center;
    padding: 2rem;
    box-sizing: border-box;
}

@media (max-width: 768px) {
    .zayne-layout {
        grid-template-columns: 1fr;
        grid-template-areas: "main";
    }

    .zayne-sidebar {
        position: fixed;
        top: 0;
        left: 0;
        height: 100%;
        z-index: 50;
        transform: translateX(-100%);
        transition: transform 260ms var(--ease-out-smooth);
        width: 260px !important;
    }

    .zayne-sidebar.is-open {
        transform: translateX(0);
    }

    .zayne-mobile-toggle {
        display: flex;
        position: fixed;
        bottom: 1.5rem;
        left: 1.5rem;
        z-index: 60;
        width: 48px;
        height: 48px;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        border: none;
        cursor: pointer;
        box-shadow: var(--zayne-custom-layout-shadow);
        background: var(--zayne-color-primary);
        color: var(--zayne-color-primary-content);
    }
}

@media (min-width: 769px) {
    .zayne-mobile-toggle,
    .zayne-mobile-backdrop {
        display: none;
    }
}

/* ============================================================
|  SECTION 3 - ANIMATIONS
============================================================ */

@keyframes zayne-modal-in { from { opacity: 0; transform: translate(-50%, -48%) scale(0.97); } to { opacity: 1; transform: translate(-50%, -50%) scale(1); } }
@keyframes zayne-modal-out { from { opacity: 1; transform: translate(-50%, -50%) scale(1); } to { opacity: 0; transform: translate(-50%, -52%) scale(0.97); } }
@keyframes zayne-fade-in { from { opacity: 0; } to { opacity: 1; } }
@keyframes zayne-fade-out { from { opacity: 1; } to { opacity: 0; } }
@keyframes zayne-drawer-in { from { transform: translateX(-100%); } to { transform: translateX(0); } }
@keyframes zayne-drawer-out { from { transform: translateX(0); } to { transform: translateX(-100%); } }
@keyframes zayne-slide-down { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: translateY(0); } }
@keyframes zayne-slide-up { from { opacity: 1; transform: translateY(0); } to { opacity: 0; transform: translateY(-6px); } }

.zayne-modal-enter { animation: zayne-modal-in 260ms var(--ease-out-smooth) forwards; }
.zayne-modal-leave { animation: zayne-modal-out 140ms var(--ease-in-smooth) backwards; }
.zayne-backdrop-enter { animation: zayne-fade-in 180ms var(--ease-out-smooth) forwards; }
.zayne-backdrop-leave { animation: zayne-fade-out 120ms var(--ease-in-smooth) backwards; }
.zayne-drawer-enter { animation: zayne-drawer-in 260ms var(--ease-out-smooth) forwards; }
.zayne-drawer-leave { animation: zayne-drawer-out 200ms var(--ease-in-smooth) backwards; }
.zayne-dropdown-enter { animation: zayne-slide-down 180ms var(--ease-out-smooth) forwards; }
.zayne-dropdown-leave { animation: zayne-slide-up 120ms var(--ease-in-smooth) backwards; }

========== ./stubs/resources/js/zayne.js ==========
document.addEventListener('alpine:init', () => {
    Alpine.data('zayneLayout', () => ({
        sidebarCollapsed: false,
        mobileOpen: false,
    }));

    Alpine.data('zayneModal', () => ({
        open: false,
    }));

    Alpine.data('zayneDrawer', () => ({
        open: false,
    }));

    Alpine.data('zayneDropdown', () => ({
        open: false,
    }));

    Alpine.data('zayneTooltip', () => ({
        open: false,
    }));

    Alpine.data('zaynePopover', () => ({
        open: false,
    }));
});

========== ./stubs/resources/views/components/zayne/alert.blade.php ==========
<div class="zayne-alert" style="{{ $style }}" {{ $attributes }}>
    @isset($iconslot)
        <span class="zayne-alert-icon">{{ $iconslot }}</span>
    @endisset

    <div class="zayne-alert-content">{{ $slot }}</div>

    @isset($trailing)
        <div class="zayne-alert-trailing">{{ $trailing }}</div>
    @endisset
</div>

========== ./stubs/resources/views/components/zayne/avatar.blade.php ==========
<div class="zayne-avatar zayne-avatar--{{ $size }}" style="{{ $style }}" {{ $attributes }}>
    @if($src !== null)
        <img src="{{ $src }}" alt="{{ $alt }}">
    @else
        <span>{{ strtoupper(substr($alt, 0, 1)) }}</span>
    @endif
</div>

========== ./stubs/resources/views/components/zayne/badge.blade.php ==========
<span class="zayne-badge zayne-badge--{{ $size }}" style="{{ $style }}" {{ $attributes }}>
    @isset($iconslot)
        <span class="zayne-badge-icon">{{ $iconslot }}</span>
    @endisset

    <span>{{ $slot }}</span>
</span>

========== ./stubs/resources/views/components/zayne/button.blade.php ==========
@php($tag = $href !== null ? 'a' : 'button')
@php($computedStyle = $style)

<{{ $tag }}
    class="zayne-button zayne-button--{{ $size }}"
    style="{{ $computedStyle }}"
    @if($href !== null) href="{{ $href }}" @else type="button" @endif
    {{ $attributes }}
>
    @isset($iconslot)
        <span class="zayne-button-icon">{{ $iconslot }}</span>
    @endisset

    <span class="zayne-button-label">{{ $slot }}</span>

    @isset($trailing)
        <span class="zayne-button-trailing">{{ $trailing }}</span>
    @endisset
</{{ $tag }}>

========== ./stubs/resources/views/components/zayne/card.blade.php ==========
<div class="zayne-card" style="{{ $style }}" {{ $attributes }}>
    @isset($header)
        <div class="zayne-card-header">{{ $header }}</div>
    @endisset

    <div class="zayne-card-body">{{ $slot }}</div>

    @isset($footer)
        <div class="zayne-card-footer">{{ $footer }}</div>
    @endisset
</div>

========== ./stubs/resources/views/components/zayne/checkbox.blade.php ==========
<label class="zayne-checkbox" {{ $attributes->except(['class', 'style']) }}>
    <span class="zayne-checkbox-box" style="{{ $style }}">
        <input type="checkbox" @checked($checked) {{ $attributes->only(['name', 'value', 'id']) }}>
        <svg viewBox="0 0 16 16" aria-hidden="true">
            <path d="M3 8.5L6.5 12L13 4.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
        </svg>
    </span>
    <span class="zayne-checkbox-label">{{ $slot }}</span>
</label>

========== ./stubs/resources/views/components/zayne/drawer.blade.php ==========
<div x-data="zayneDrawer()" class="zayne-drawer-root" {{ $attributes->except(['class', 'style']) }}>
    @isset($trigger)
        <div x-on:click="open = true">{{ $trigger }}</div>
    @endisset

    <div
        x-show="open"
        x-cloak
        x-on:click="open = false"
        x-bind:class="open ? 'zayne-backdrop-enter' : 'zayne-backdrop-leave'"
        class="zayne-backdrop"
    ></div>

    <div
        x-show="open"
        x-cloak
        x-trap="open"
        x-bind:class="open ? 'zayne-drawer-enter' : 'zayne-drawer-leave'"
        class="zayne-drawer-panel"
        style="{{ $style }}"
    >
        {{ $slot }}
    </div>
</div>

========== ./stubs/resources/views/components/zayne/dropdown.blade.php ==========
<div x-data="zayneDropdown()" class="zayne-dropdown-root" {{ $attributes->except(['class', 'style']) }}>
    @isset($trigger)
        <div x-on:click="open = !open">{{ $trigger }}</div>
    @endisset

    <div
        x-show="open"
        x-cloak
        x-on:click.outside="open = false"
        x-bind:class="open ? 'zayne-dropdown-enter' : 'zayne-dropdown-leave'"
        class="zayne-dropdown-panel"
        style="{{ $style }}"
    >
        {{ $slot }}
    </div>
</div>

========== ./stubs/resources/views/components/zayne/header/avatar.blade.php ==========
<div class="zayne-header-avatar" {{ $attributes }}>
    <zayne:avatar :src="$src" :alt="$alt" size="sm" />
    @if($label !== null)
        <span>{{ $label }}</span>
    @endif
</div>

========== ./stubs/resources/views/components/zayne/header/brand.blade.php ==========
@php($tag = $href !== null ? 'a' : 'div')
<{{ $tag }} class="zayne-brand" @if($href !== null) href="{{ $href }}" @endif {{ $attributes }}>
    {{ $name }}
</{{ $tag }}>

========== ./stubs/resources/views/components/zayne/header/nav.blade.php ==========
<nav class="zayne-header-nav" style="gap: {{ $gap }};" {{ $attributes }}>
    {{ $slot }}
</nav>

========== ./stubs/resources/views/components/zayne/input.blade.php ==========
<input
    class="zayne-input"
    style="{{ $style }}"
    type="{{ $type }}"
    @if($value !== null) value="{{ $value }}" @endif
    @if($placeholder !== null) placeholder="{{ $placeholder }}" @endif
    {{ $attributes }}
>

========== ./stubs/resources/views/components/zayne/layout.blade.php ==========
<div class="zayne-layout" x-data="zayneLayout()">
    @isset($sidebar)
        <aside class="zayne-sidebar" x-bind:class="{ 'is-collapsed': sidebarCollapsed, 'is-open': mobileOpen }">
            {{ $sidebar }}
        </aside>
    @endisset

    <div class="zayne-main-wrapper">
        @isset($header)
            {{ $header }}
        @endisset

        <main class="zayne-main">{{ $slot }}</main>
    </div>

    <div
        class="zayne-mobile-backdrop"
        x-show="mobileOpen"
        x-cloak
        x-on:click="mobileOpen = false"
        x-bind:class="mobileOpen ? 'zayne-backdrop-enter' : 'zayne-backdrop-leave'"
    ></div>

    <button
        class="zayne-mobile-toggle"
        x-on:click="mobileOpen = !mobileOpen"
        aria-label="Toggle navigation"
        type="button"
    >
        <svg x-show="!mobileOpen" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="3" y1="6" x2="21" y2="6" />
            <line x1="3" y1="12" x2="21" y2="12" />
            <line x1="3" y1="18" x2="21" y2="18" />
        </svg>
        <svg x-show="mobileOpen" x-cloak xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="18" y1="6" x2="6" y2="18" />
            <line x1="6" y1="6" x2="18" y2="18" />
        </svg>
    </button>
</div>

========== ./stubs/resources/views/components/zayne/layout/header.blade.php ==========
<header
    class="zayne-header"
    style="
        background: {{ $background }};
        box-shadow: {{ $shadow }};
        padding: {{ $padding }};
        gap: {{ $gap }};
        margin: {{ $margin }};
        margin-top: {{ $margintop }};
        margin-bottom: {{ $marginbottom }};
        margin-left: {{ $marginleft }};
        margin-right: {{ $marginright }};
        border-width: {{ $border }};
        border-top-width: {{ $bordertop }};
        border-bottom-width: {{ $borderbottom }};
        border-left-width: {{ $borderleft }};
        border-right-width: {{ $borderright }};
        border-color: {{ $bordercolor }};
    "
    {{ $attributes }}
>
    @isset($left)
        <div class="zayne-header-left">{{ $left }}</div>
    @endisset

    <div class="zayne-header-center">{{ $slot }}</div>

    @isset($right)
        <div class="zayne-header-right">{{ $right }}</div>
    @endisset
</header>

========== ./stubs/resources/views/components/zayne/layout/main.blade.php ==========
<div
    class="zayne-main"
    style="
        margin: {{ $margin }};
        padding: {{ $padding }};
        background: {{ $background }};
    "
    {{ $attributes }}
>
    {{ $slot }}
</div>

========== ./stubs/resources/views/components/zayne/layout/sidebar.blade.php ==========
<aside
    class="zayne-layout-sidebar-shell"
    style="
        display: flex;
        flex-direction: column;
        gap: {{ $gap }};
        background: {{ $background }};
        padding: {{ $padding }};
        box-shadow: {{ $shadow }};
        border-radius: {{ $radius }};
        margin: {{ $margin }};
        margin-top: {{ $margintop }};
        margin-bottom: {{ $marginbottom }};
        margin-left: {{ $marginleft }};
        margin-right: {{ $marginright }};
        border-width: {{ $border }};
        border-top-width: {{ $bordertop }};
        border-bottom-width: {{ $borderbottom }};
        border-left-width: {{ $borderleft }};
        border-right-width: {{ $borderright }};
        border-color: {{ $bordercolor }};
    "
    {{ $attributes }}
>
    @isset($header)
        <div>{{ $header }}</div>
    @endisset

    <div class="zayne-layout-sidebar-body">{{ $slot }}</div>

    @isset($footer)
        <div>{{ $footer }}</div>
    @endisset
</aside>

========== ./stubs/resources/views/components/zayne/modal.blade.php ==========
<div x-data="zayneModal()" class="zayne-modal-root" {{ $attributes->except(['class', 'style']) }}>
    @isset($trigger)
        <div x-on:click="open = true">{{ $trigger }}</div>
    @endisset

    <div
        x-show="open"
        x-cloak
        x-on:click="open = false"
        x-bind:class="open ? 'zayne-backdrop-enter' : 'zayne-backdrop-leave'"
        class="zayne-backdrop"
    ></div>

    <div
        x-show="open"
        x-cloak
        x-trap="open"
        x-bind:class="open ? 'zayne-modal-enter' : 'zayne-modal-leave'"
        class="zayne-modal-panel"
        style="{{ $style }}"
    >
        {{ $slot }}
    </div>
</div>

========== ./stubs/resources/views/components/zayne/popover.blade.php ==========
<div x-data="zaynePopover()" class="zayne-popover-root" {{ $attributes->except(['class', 'style']) }}>
    @isset($trigger)
        <div x-on:click="open = !open">{{ $trigger }}</div>
    @endisset

    <div
        x-show="open"
        x-cloak
        x-on:click.outside="open = false"
        x-bind:class="open ? 'zayne-dropdown-enter' : 'zayne-dropdown-leave'"
        class="zayne-popover-panel"
        style="{{ $style }}"
    >
        {{ $slot }}
    </div>
</div>

========== ./stubs/resources/views/components/zayne/progress.blade.php ==========
<div class="zayne-progress" style="{{ $style }}" {{ $attributes }}>
    <div class="zayne-progress-bar" style="{{ $barStyle }}"></div>
</div>

========== ./stubs/resources/views/components/zayne/select.blade.php ==========
<select class="zayne-input zayne-select" style="{{ $style }}" {{ $attributes }}>
    {{ $slot }}
</select>

========== ./stubs/resources/views/components/zayne/sidebar/avatar.blade.php ==========
<div class="zayne-sidebar-avatar" {{ $attributes }}>
    <zayne:avatar :src="$src" :alt="$alt" size="md" />
    @if($label !== null)
        <span class="sidebar-label">{{ $label }}</span>
    @endif
</div>

========== ./stubs/resources/views/components/zayne/sidebar/brand.blade.php ==========
@php($tag = $href !== null ? 'a' : 'div')
<{{ $tag }} class="zayne-sidebar-brand" @if($href !== null) href="{{ $href }}" @endif {{ $attributes }}>
    {{ $name }}
</{{ $tag }}>

========== ./stubs/resources/views/components/zayne/sidebar/label.blade.php ==========
<div class="zayne-sidebar-label" {{ $attributes }}>{{ $slot }}</div>

========== ./stubs/resources/views/components/zayne/sidebar/navitem.blade.php ==========
@php($tag = $href !== null ? 'a' : 'button')
<{{ $tag }}
    class="zayne-navitem{{ $active ? ' active' : '' }}"
    @if($href !== null) href="{{ $href }}" @else type="button" @endif
    {{ $attributes }}
>
    @isset($iconslot)
        <span class="zayne-icon-wrapper">{{ $iconslot }}</span>
    @endisset

    <span class="sidebar-label">{{ $slot }}</span>

    @isset($trailing)
        <span>{{ $trailing }}</span>
    @endisset
</{{ $tag }}>

========== ./stubs/resources/views/components/zayne/sidebar/navtree.blade.php ==========
<div class="zayne-navtree" {{ $attributes }}>
    @if($title !== null)
        <div class="zayne-sidebar-label">{{ $title }}</div>
    @endif

    <div class="zayne-navtree-items">{{ $slot }}</div>
</div>

========== ./stubs/resources/views/components/zayne/sidebar/navtreeitem.blade.php ==========
@php($tag = $href !== null ? 'a' : 'button')
<{{ $tag }}
    class="zayne-navitem zayne-navtreeitem{{ $active ? ' active' : '' }}"
    @if($href !== null) href="{{ $href }}" @else type="button" @endif
    {{ $attributes }}
>
    @isset($iconslot)
        <span class="zayne-icon-wrapper">{{ $iconslot }}</span>
    @endisset

    <span class="sidebar-label">{{ $slot }}</span>

    @isset($trailing)
        <span>{{ $trailing }}</span>
    @endisset
</{{ $tag }}>

========== ./stubs/resources/views/components/zayne/toggle.blade.php ==========
<label class="zayne-toggle" {{ $attributes->except(['class', 'style']) }}>
    <span class="zayne-toggle-track" style="{{ $style }}">
        <input type="checkbox" @checked($checked) {{ $attributes->only(['name', 'value', 'id']) }}>
        <span class="zayne-toggle-thumb{{ $checked ? ' is-on' : '' }}"></span>
    </span>
    <span class="zayne-toggle-label">{{ $slot }}</span>
</label>

========== ./stubs/resources/views/components/zayne/tooltip.blade.php ==========
<div
    x-data="zayneTooltip()"
    class="zayne-tooltip-root"
    x-on:mouseenter="open = true"
    x-on:mouseleave="open = false"
    {{ $attributes->except(['class', 'style']) }}
>
    @isset($trigger)
        <div>{{ $trigger }}</div>
    @endisset

    <div
        x-show="open"
        x-cloak
        x-bind:class="open ? 'zayne-dropdown-enter' : 'zayne-dropdown-leave'"
        class="zayne-tooltip-panel"
        style="{{ $style }}"
    >
        {{ $slot->isNotEmpty() ? $slot : $text }}
    </div>
</div>

========== ./stubs/resources/views/layouts/app/header-only.blade.php ==========
<!DOCTYPE html>
<html class="light" lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    @zayneAppearance
    @zayneStyles
</head>
<body>
    @isset($header)
        {{ $header }}
    @endisset
    {{ $slot }}
    @zayneScripts
</body>
</html>

========== ./stubs/resources/views/layouts/app/sidebar-header.blade.php ==========
<!DOCTYPE html>
<html class="light" lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    @zayneAppearance
    @zayneStyles
</head>
<body>
    <zayne:layout>
        <x-slot:sidebar>{{ $sidebar ?? '' }}</x-slot:sidebar>
        <x-slot:header>{{ $header ?? '' }}</x-slot:header>
        {{ $slot }}
    </zayne:layout>
    @zayneScripts
</body>
</html>

========== ./stubs/resources/views/layouts/app/sidebar-only.blade.php ==========
<!DOCTYPE html>
<html class="light" lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    @zayneAppearance
    @zayneStyles
</head>
<body>
    <zayne:layout>
        <x-slot:sidebar>{{ $sidebar ?? '' }}</x-slot:sidebar>
        {{ $slot }}
    </zayne:layout>
    @zayneScripts
</body>
</html>

========== ./stubs/resources/views/layouts/auth/base.blade.php ==========
<!DOCTYPE html>
<html class="light" lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    @zayneAppearance
    @zayneStyles
</head>
<body>
    <main class="zayne-auth-shell">
        {{ $slot }}
    </main>
    @zayneScripts
</body>
</html>

========== ./zayne_source.txt ==========

========== ./Zayneui-Architecture-guide.txt ==========
# ZAYNE UI — FULL ARCHITECTURE GUIDE
# For Codex: Complete package rebuild from scratch

---

## WHAT THIS IS

Zayne UI is a CSS token-driven component engine for Laravel + Blade.
It is a **Composer package** (`zayne/ui`) that works immediately after install.
No `php artisan zayne:install` required to use components.
No stub copying. No file generation.

The user adds two directives and uses components instantly:

```blade
<head>@zayneStyles</head>
<body>
    <zayne:button variant="solid" color="primary">Save</zayne:button>
    @zayneScripts
</body>
```

---

## CORE PRINCIPLES

- PHP class owns props + variant resolution
- Blade renders structure only — zero logic
- CSS variables drive all theming
- One CSS file. One JS file.
- Alpine.js only for interactive DOM state (Overlay components + root layout)
- All Alpine x-data definitions live in zayne.js — never inline in Blade
- Zero Tailwind dependency
- No base class. No traits. No external variant maps.
- Variant map lives inline inside mount() of each component
- Package-owned components — works on composer install, no publishing required
- Publishing is optional — only needed when user wants to customize

---

## FINAL FILE STRUCTURE

```
ZayneUI/
├── composer.json
├── README.md
├── src/
│   ├── Components/
│   │   ├── Layout/
│   │   │   ├── Header.php
│   │   │   ├── Main.php
│   │   │   └── Sidebar.php
│   │   ├── Header/
│   │   │   ├── Brand.php
│   │   │   ├── Avatar.php
│   │   │   └── Nav.php
│   │   ├── Sidebar/
│   │   │   ├── Brand.php
│   │   │   ├── Avatar.php
│   │   │   ├── Label.php
│   │   │   ├── Navitem.php
│   │   │   ├── Navtree.php
│   │   │   └── Navtreeitem.php
│   │   ├── Button.php
│   │   ├── Badge.php
│   │   ├── Alert.php
│   │   ├── Modal.php
│   │   ├── Drawer.php
│   │   ├── Dropdown.php
│   │   ├── Tooltip.php
│   │   ├── Popover.php
│   │   ├── Input.php
│   │   ├── Select.php
│   │   ├── Checkbox.php
│   │   ├── Toggle.php
│   │   ├── Avatar.php
│   │   ├── Card.php
│   │   └── Progress.php
│   ├── Console/
│   │   ├── InstallCommand.php
│   │   └── PublishCommand.php
│   ├── ZayneAssetManager.php
│   ├── ZayneManager.php
│   ├── ZayneTagCompiler.php
│   ├── ZayneServiceProvider.php
│   └── Zayne.php
└── stubs/
    └── resources/
        ├── css/
        │   └── zayne.css
        ├── js/
        │   └── zayne.js
        └── views/
            ├── components/
            │   └── zayne/
            │       ├── layout/
            │       │   ├── header.blade.php
            │       │   ├── main.blade.php
            │       │   └── sidebar.blade.php
            │       ├── header/
            │       │   ├── brand.blade.php
            │       │   ├── avatar.blade.php
            │       │   └── nav.blade.php
            │       ├── sidebar/
            │       │   ├── brand.blade.php
            │       │   ├── avatar.blade.php
            │       │   ├── label.blade.php
            │       │   ├── navitem.blade.php
            │       │   ├── navtree.blade.php
            │       │   └── navtreeitem.blade.php
            │       ├── button.blade.php
            │       ├── badge.blade.php
            │       ├── alert.blade.php
            │       ├── modal.blade.php
            │       ├── drawer.blade.php
            │       ├── dropdown.blade.php
            │       ├── tooltip.blade.php
            │       ├── popover.blade.php
            │       ├── input.blade.php
            │       ├── select.blade.php
            │       ├── checkbox.blade.php
            │       ├── toggle.blade.php
            │       ├── avatar.blade.php
            │       ├── card.blade.php
            │       └── progress.blade.php
            └── layouts/
                ├── app/
                │   ├── sidebar-header.blade.php
                │   ├── header-only.blade.php
                │   └── sidebar-only.blade.php
                └── auth/
                    └── base.blade.php
```

---

## COMPOSER.JSON

```json
{
    "name": "zayne/ui",
    "description": "CSS token-driven UI component engine for Laravel",
    "type": "library",
    "require": {
        "php": "^8.1",
        "illuminate/support": "^10.0|^11.0|^12.0",
        "illuminate/view": "^10.0|^11.0|^12.0"
    },
    "autoload": {
        "psr-4": {
            "Zayne\\UI\\": "src/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "Zayne\\UI\\ZayneServiceProvider"
            ],
            "aliases": {
                "Zayne": "Zayne\\UI\\Zayne"
            }
        }
    }
}
```

---

## src/Zayne.php — FACADE

```php
<?php

namespace Zayne\UI;

use Illuminate\Support\Facades\Facade;

/**
 * @see \Zayne\UI\ZayneManager
 */
class Zayne extends Facade
{
    public static function getFacadeAccessor(): string
    {
        return 'zayne';
    }
}
```

---

## src/ZayneManager.php — SINGLETON

```php
<?php

namespace Zayne\UI;

use Illuminate\Support\Str;
use Illuminate\View\ComponentAttributeBag;

class ZayneManager
{
    /**
     * Convert a CSS property array into an inline style string.
     * Filters out null and empty values.
     *
     * ['background' => 'red', 'color' => 'white']
     * → "background: red; color: white"
     */
    public function styleString(array $styles): string
    {
        return collect($styles)
            ->filter(fn($value) => $value !== null && $value !== '')
            ->map(fn($value, $key) => "{$key}: {$value}")
            ->implode('; ');
    }

    /**
     * Split an attribute bag into two bags:
     * one containing keys that start with $by, one containing the rest.
     */
    public function splitAttributes(ComponentAttributeBag $attributes, array $by = ['class', 'style'], bool $strict = false): array
    {
        return [
            $strict ? $attributes->only($by) : $attributes->whereStartsWith($by),
            $strict ? $attributes->except($by) : $attributes->whereDoesntStartWith($by),
        ];
    }

    /**
     * Extract named props from the attribute bag.
     * Unescapes HTML entities because Blade auto-escapes attribute values.
     * Also handles kebab-case → camelCase conversion.
     */
    public function forwardedAttributes(ComponentAttributeBag $attributes, array $propKeys): array
    {
        $props = [];
        $unescape = fn($value) => is_string($value) ? htmlspecialchars_decode($value, ENT_QUOTES) : $value;

        foreach ($propKeys as $key) {
            if (isset($attributes[$key])) {
                $props[$key] = $unescape($attributes[$key]);
            } elseif (isset($attributes[Str::kebab($key)])) {
                $props[$key] = $unescape($attributes[Str::kebab($key)]);
            }
        }

        return $props;
    }

    /**
     * Strip a prefix from attribute names and move them into a new bag.
     * Example: 'icon:variant' with prefix 'icon:' → 'variant' in new bag.
     */
    public function attributesAfter(string $prefix, ComponentAttributeBag $attributes, array $default = []): ComponentAttributeBag
    {
        $newAttributes = new ComponentAttributeBag($default);
        $keysToRemove = [];

        foreach ($attributes->getAttributes() as $key => $value) {
            if (str_starts_with($key, $prefix)) {
                $newAttributes[substr($key, strlen($prefix))] = $value;
                $keysToRemove[] = $key;
            }
        }

        foreach ($keysToRemove as $key) {
            unset($attributes[$key]);
        }

        return $newAttributes;
    }
}
```

---

## src/ZayneServiceProvider.php

```php
<?php

namespace Zayne\UI;

use Illuminate\Foundation\AliasLoader;
use Illuminate\Support\Facades\Blade;
use Illuminate\Support\ServiceProvider;
use Illuminate\View\ComponentAttributeBag;

class ZayneServiceProvider extends ServiceProvider
{
    public function register(): void
    {
        $this->app->singleton(ZayneManager::class);
        $this->app->alias(ZayneManager::class, 'zayne');

        $loader = AliasLoader::getInstance();
        $loader->alias('Zayne', Zayne::class);
    }

    public function boot(): void
    {
        $this->bootComponents();
        $this->bootTagCompiler();
        $this->bootAssetManager();
        $this->bootDirectives();
        $this->bootMacros();
        $this->bootCommands();
    }

    /**
     * Register component namespace + blade view paths.
     * User-published path is registered first so it always wins.
     */
    protected function bootComponents(): void
    {
        // PHP classes — <zayne:button> → Zayne\UI\Components\Button
        Blade::componentNamespace('Zayne\\UI\\Components', 'zayne');

        // User-published Blade views override package views
        if (file_exists(resource_path('views/components/zayne'))) {
            Blade::anonymousComponentPath(
                resource_path('views/components/zayne'),
                'zayne'
            );
        }

        // Package Blade views — fallback
        Blade::anonymousComponentPath(
            __DIR__ . '/../stubs/resources/views/components/zayne',
            'zayne'
        );
    }

    /**
     * Register ZayneTagCompiler as a Blade precompiler.
     * Enables <zayne:button> syntax in all Blade templates.
     */
    protected function bootTagCompiler(): void
    {
        $compiler = new ZayneTagCompiler(
            app('blade.compiler')->getClassComponentAliases(),
            app('blade.compiler')->getClassComponentNamespaces(),
            app('blade.compiler')
        );

        app()->bind('zayne.compiler', fn() => $compiler);

        app('blade.compiler')->precompiler(function ($value) use ($compiler) {
            return $compiler->compile($value);
        });
    }

    /**
     * Boot asset routes and Blade directives.
     */
    protected function bootAssetManager(): void
    {
        ZayneAssetManager::boot();
    }

    /**
     * Register @zayneStyles, @zayneScripts, @zayneAppearance directives.
     */
    protected function bootDirectives(): void
    {
        Blade::directive('zayneStyles', function () {
            return '{!! app(\'zayne\')->renderStyles() !!}';
        });

        Blade::directive('zayneScripts', function () {
            return '{!! app(\'zayne\')->renderScripts() !!}';
        });

        Blade::directive('zayneAppearance', function () {
            return '{!! app(\'zayne\')->renderAppearance() !!}';
        });
    }

    /**
     * Add AttributeBag::pluck() macro.
     * Extracts and removes an attribute in one shot.
     */
    protected function bootMacros(): void
    {
        ComponentAttributeBag::macro('pluck', function ($key, $default = null) {
            $result = $this->get($key);
            unset($this->attributes[$key]);
            return $result ?? $default;
        });
    }

    protected function bootCommands(): void
    {
        if (! $this->app->runningInConsole()) {
            return;
        }

        $this->commands([
            Console\InstallCommand::class,
            Console\PublishCommand::class,
        ]);
    }
}
```

---

## src/ZayneTagCompiler.php

```php
<?php

namespace Zayne\UI;

use Illuminate\View\Compilers\ComponentTagCompiler;

class ZayneTagCompiler extends ComponentTagCompiler
{
    /**
     * Compile opening tags: <zayne:button ...>
     */
    protected function compileOpeningTags(string $value): string
    {
        $pattern = "/
            <
                \s*
                zayne[\:][\w\-\:\.]* 
                (?<attributes>
                    (?:
                        \s+
                        (?:
                            (?:
                                @(?:class)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                @(?:style)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                \{\{\s*\\\$attributes(?:[^}]+?)?\s*\}\}
                            )
                            |
                            (?:
                                [\w\-:.@%]+
                                (
                                    =
                                    (?:
                                        \\\"[^\\\"]*\\\"
                                        |
                                        \'[^\']*\'
                                        |
                                        [^\'\\\"=<>]+
                                    )
                                )?
                            )
                        )
                    )*
                    \s*
                )
                (?<![\/=\-])
            >
        /x";

        return preg_replace_callback($pattern, function (array $matches) {
            $this->boundAttributes = [];
            $component = 'zayne::' . $this->extractComponentName($matches[0]);
            $attributes = $this->getAttributesFromAttributeString($matches['attributes']);
            return $this->componentString($component, $attributes);
        }, $value);
    }

    /**
     * Compile self-closing tags: <zayne:button />
     */
    protected function compileSelfClosingTags(string $value): string
    {
        $pattern = "/
            <
                \s*
                zayne[\:]([\w\-\:\.]*)
                \s*
                (?<attributes>
                    (?:
                        \s+
                        (?:
                            (?:
                                @(?:class)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                @(?:style)(\( (?: (?>[^()]+) | (?-1) )* \))
                            )
                            |
                            (?:
                                \{\{\s*\\\$attributes(?:[^}]+?)?\s*\}\}
                            )
                            |
                            (?:
                                [\w\-:.@%]+
                                (
                                    =
                                    (?:
                                        \\\"[^\\\"]*\\\"
                                        |
                                        \'[^\']*\'
                                        |
                                        [^\'\\\"=<>]+
                                    )
                                )?
                            )
                        )
                    )*
                    \s*
                )
            \/>
        /x";

        return preg_replace_callback($pattern, function (array $matches) {
            $this->boundAttributes = [];
            $attributes = $this->getAttributesFromAttributeString($matches['attributes']);

            if (isset($attributes['slot'])) {
                $slot = $attributes['slot'];
                unset($attributes['slot']);
                return '@slot(' . $slot . ') '
                    . $this->componentString('zayne::' . $matches[1], $attributes)
                    . "\n@endComponentClass##END-COMPONENT-CLASS##"
                    . ' @endslot';
            }

            return $this->componentString('zayne::' . $matches[1], $attributes)
                . "\n@endComponentClass##END-COMPONENT-CLASS##";
        }, $value);
    }

    /**
     * Compile closing tags: </zayne:button>
     */
    protected function compileClosingTags(string $value): string
    {
        return preg_replace(
            "/<\/\s*zayne[\:][\w\-\:\.]*\s*>/",
            ' @endComponentClass##END-COMPONENT-CLASS##',
            $value
        );
    }

    /**
     * Extract component name from the opening tag match.
     */
    protected function extractComponentName(string $tag): string
    {
        preg_match('/<\s*zayne[\:]([\w\-\:\.]*)/i', $tag, $matches);
        return $matches[1] ?? '';
    }
}
```

---

## src/ZayneAssetManager.php

```php
<?php

namespace Zayne\UI;

use Illuminate\Http\Request;
use Illuminate\Support\Facades\Route;

class ZayneAssetManager
{
    public static function boot(): void
    {
        $instance = new static;
        $instance->registerRoutes();
    }

    protected function registerRoutes(): void
    {
        Route::get('/zayne/zayne.css', [static::class, 'css']);
        Route::get('/zayne/zayne.js',  [static::class, 'js']);
    }

    public function css(): mixed
    {
        return $this->pretendResponseIsFile(
            __DIR__ . '/../stubs/resources/css/zayne.css',
            'text/css'
        );
    }

    public function js(): mixed
    {
        return $this->pretendResponseIsFile(
            __DIR__ . '/../stubs/resources/js/zayne.js',
            'text/javascript'
        );
    }

    public static function renderStyles(): string
    {
        $version = filemtime(__DIR__ . '/../stubs/resources/css/zayne.css');
        return '<link rel="stylesheet" href="' . url('/zayne/zayne.css?v=' . $version) . '">';
    }

    public static function renderScripts(): string
    {
        $version = filemtime(__DIR__ . '/../stubs/resources/js/zayne.js');
        return '<script src="' . url('/zayne/zayne.js?v=' . $version) . '" defer></script>';
    }

    public static function renderAppearance(): string
    {
        return <<<HTML
<script>
    (function() {
        var saved = localStorage.getItem('zayne.theme') || 'light';
        document.documentElement.className = saved;
    })();
</script>
HTML;
    }

    protected function pretendResponseIsFile(string $file, string $contentType): mixed
    {
        $lastModified = filemtime($file);

        return $this->cachedFileResponse($file, $contentType, $lastModified,
            fn($headers) => response()->file($file, $headers)
        );
    }

    protected function cachedFileResponse(string $filename, string $contentType, int $lastModified, callable $downloadCallback): mixed
    {
        $expires = strtotime('+1 year');
        $cacheControl = 'public, max-age=31536000';

        if ($this->matchesCache($lastModified)) {
            return response('', 304, [
                'Expires'       => $this->httpDate($expires),
                'Cache-Control' => $cacheControl,
            ]);
        }

        return $downloadCallback([
            'Content-Type'  => $contentType,
            'Expires'       => $this->httpDate($expires),
            'Cache-Control' => $cacheControl,
            'Last-Modified' => $this->httpDate($lastModified),
        ]);
    }

    protected function matchesCache(int $lastModified): bool
    {
        $ifModifiedSince = app(Request::class)->header('if-modified-since');
        return $ifModifiedSince !== null && @strtotime($ifModifiedSince) === $lastModified;
    }

    protected function httpDate(int $timestamp): string
    {
        return sprintf('%s GMT', gmdate('D, d M Y H:i:s', $timestamp));
    }
}
```

---

## src/Console/InstallCommand.php

Simplified. No stub copying. Just wires CSS/JS into the app.

```php
<?php

namespace Zayne\UI\Console;

use Illuminate\Console\Command;
use Illuminate\Filesystem\Filesystem;
use Illuminate\Support\Str;
use Symfony\Component\Console\Attribute\AsCommand;

#[AsCommand(name: 'zayne:install')]
class InstallCommand extends Command
{
    protected $signature = 'zayne:install';
    protected $description = 'Wire ZayneUI directives into your Laravel app.';

    protected Filesystem $files;

    public function __construct(Filesystem $files)
    {
        parent::__construct();
        $this->files = $files;
    }

    public function handle(): int
    {
        $this->components->info('Setting up ZayneUI...');
        $this->newLine();

        $this->ensureAlpineJs();
        $this->registerServiceProvider();

        $this->newLine();
        $this->components->info('ZayneUI is ready.');
        $this->newLine();
        $this->line('  <fg=gray>Add to your layout:</> ');
        $this->line('  <fg=cyan>@zayneStyles</> inside <fg=cyan><head></>');
        $this->line('  <fg=cyan>@zayneScripts</> before <fg=cyan></body></>');
        $this->line('  <fg=cyan>@zayneAppearance</> inside <fg=cyan><head></> (optional, for theme persistence)');
        $this->newLine();
        $this->line('  <fg=gray>Set theme on <html>:</> <fg=cyan>class="light"</> | <fg=cyan>class="dark"</> | <fg=cyan>class="abyss"</>');
        $this->newLine();

        return self::SUCCESS;
    }

    protected function ensureAlpineJs(): void
    {
        $packageJsonPath = base_path('package.json');

        if (! $this->files->exists($packageJsonPath)) {
            $this->components->warn('package.json not found. Install Alpine.js manually: npm install alpinejs');
            return;
        }

        $decoded = json_decode($this->files->get($packageJsonPath), true);
        $all = array_merge($decoded['dependencies'] ?? [], $decoded['devDependencies'] ?? []);

        if (array_key_exists('alpinejs', $all)) {
            $this->components->twoColumnDetail('<fg=green>Found</> Alpine.js', '<fg=gray>alpinejs</>');
            return;
        }

        $this->components->warn('Alpine.js not found in package.json.');
        $this->line('  Run: <fg=cyan>npm install alpinejs</>');
    }

    protected function registerServiceProvider(): void
    {
        $bootstrapPath = base_path('bootstrap/providers.php');

        if (! $this->files->exists($bootstrapPath)) {
            $this->components->warn('bootstrap/providers.php not found. ZayneUI auto-discovers via composer.json.');
            return;
        }

        // Laravel 11+ auto-discovers via composer.json extra.laravel.providers
        // Nothing to register manually
        $this->components->twoColumnDetail(
            '<fg=green>Auto-discovered</> ZayneServiceProvider',
            '<fg=gray>via composer.json</>',
        );
    }
}
```

---

## src/Console/PublishCommand.php

Lets users copy out specific components to override them.

```php
<?php

namespace Zayne\UI\Console;

use Illuminate\Console\Command;
use Illuminate\Filesystem\Filesystem;
use Symfony\Component\Console\Attribute\AsCommand;

#[AsCommand(name: 'zayne:publish')]
class PublishCommand extends Command
{
    protected $signature = 'zayne:publish
                            {components?* : Component name(s) to publish}
                            {--all : Publish all components}
                            {--force : Overwrite existing files}';

    protected $description = 'Publish Zayne UI components for customization.';

    protected Filesystem $files;

    // All publishable components
    protected array $components = [
        'button', 'badge', 'alert', 'avatar', 'card', 'progress',
        'modal', 'drawer', 'dropdown', 'tooltip', 'popover',
        'input', 'select', 'checkbox', 'toggle',
        'layout/header', 'layout/main', 'layout/sidebar',
        'header/brand', 'header/avatar', 'header/nav',
        'sidebar/brand', 'sidebar/avatar', 'sidebar/label',
        'sidebar/navitem', 'sidebar/navtree', 'sidebar/navtreeitem',
    ];

    public function __construct(Filesystem $files)
    {
        parent::__construct();
        $this->files = $files;
    }

    public function handle(): int
    {
        $targets = $this->option('all')
            ? $this->components
            : ($this->argument('components') ?: $this->askWhichComponent());

        foreach ($targets as $component) {
            $this->publishComponent($component);
        }

        return self::SUCCESS;
    }

    protected function askWhichComponent(): array
    {
        $choice = $this->choice(
            'Which component would you like to publish?',
            $this->components
        );

        return [$choice];
    }

    protected function publishComponent(string $component): void
    {
        $force = $this->option('force');

        // Publish Blade view
        $bladeSrc  = __DIR__ . '/../../stubs/resources/views/components/zayne/' . $component . '.blade.php';
        $bladeDest = resource_path('views/components/zayne/' . $component . '.blade.php');

        if ($this->files->exists($bladeSrc)) {
            if ($this->files->exists($bladeDest) && ! $force) {
                $this->components->twoColumnDetail(
                    "<fg=yellow>Skipped</> {$component}.blade.php",
                    '<fg=gray>use --force to overwrite</>'
                );
            } else {
                $this->files->ensureDirectoryExists(dirname($bladeDest));
                $this->files->copy($bladeSrc, $bladeDest);
                $this->components->twoColumnDetail(
                    "<fg=green>Published</> {$component}.blade.php",
                    '<fg=gray>resources/views/components/zayne/</>'
                );
            }
        }

        // Publish PHP class
        $className = $this->componentToClassName($component);
        $phpSrc    = __DIR__ . '/../Components/' . $className . '.php';
        $phpDest   = app_path('View/Components/Zayne/' . $className . '.php');

        if ($this->files->exists($phpSrc)) {
            if ($this->files->exists($phpDest) && ! $force) {
                $this->components->twoColumnDetail(
                    "<fg=yellow>Skipped</> {$className}.php",
                    '<fg=gray>use --force to overwrite</>'
                );
            } else {
                $this->files->ensureDirectoryExists(dirname($phpDest));
                // Rewrite namespace for user's app
                $contents = str_replace(
                    'namespace Zayne\\UI\\Components',
                    'namespace App\\View\\Components\\Zayne',
                    $this->files->get($phpSrc)
                );
                $this->files->put($phpDest, $contents);
                $this->components->twoColumnDetail(
                    "<fg=green>Published</> {$className}.php",
                    '<fg=gray>app/View/Components/Zayne/</>'
                );
            }
        }
    }

    protected function componentToClassName(string $component): string
    {
        return collect(explode('/', $component))
            ->map(fn($part) => str($part)->studly())
            ->implode('/');
    }
}
```

---

## COMPONENT TYPES

| Type    | Role               | Variants | Alpine | mount() | Examples                            |
|---------|--------------------|----------|--------|---------|-------------------------------------|
| Layout  | Page structure     | ❌       | ❌     | ❌      | Header, Sidebar, Main               |
| Visual  | Appearance only    | ✅       | ❌     | ✅      | Badge, Avatar, Card                 |
| Action  | Behavior + Visual  | ✅       | ❌     | ✅      | Button, Toggle                      |
| Overlay | DOM state layers   | ✅       | ✅     | ✅      | Modal, Drawer, Popover, Tooltip, Dropdown |
| Data    | Display data sets  | ❌       | ❌     | ❌      | Table, List                         |
| Feed    | Status/feedback    | ✅       | ❌     | ✅      | Alert, Progress                     |
| Form    | Input collection   | ✅       | ❌     | ✅      | Input, Select, Checkbox             |

---

## COMPONENT PHP CLASS RULES

### Layout / Data components — NO mount()

```php
<?php

namespace Zayne\UI\Components\Layout;

use Illuminate\View\Component;
use Illuminate\View\View;

class Header extends Component
{
    public function __construct(
        public string $background = 'var(--zayne-custom-header)',
        public string $shadow     = 'var(--zayne-shadow)',
        public string $padding    = 'var(--zayne-padding-header)',
        public string $gap        = 'var(--zayne-gap-header)',
        public string $border     = 'unset',
        public string $bordertop  = 'unset',
        public string $borderbottom = 'unset',
        public string $bordercolor  = 'unset',
        public string $margin       = 'unset',
    ) {}

    public function render(): View
    {
        return view('zayne::layout.header');
    }
}
```

No `mount()`. No `$style`. Props output directly in Blade.

### Action / Visual / Feed / Form / Overlay — WITH mount()

```php
<?php

namespace Zayne\UI\Components;

use Illuminate\View\Component;
use Illuminate\View\View;
use Zayne\UI\Zayne;

class Button extends Component
{
    public string $style = '';

    public function __construct(
        public string  $variant    = 'solid',
        public string  $color      = 'primary',
        public string  $size       = 'md',
        public ?string $href       = null,
        public string  $padding    = 'unset',
        public string  $radius     = 'var(--zayne-radius-field)',
        public string  $shadow     = 'unset',
        public string  $gap        = 'var(--zayne-gap-button)',
        public string  $margin     = 'unset',
        public string  $border     = 'unset',
        public string  $bordercolor = 'unset',
    ) {}

    public function mount(): void
    {
        $variantStyles = [
            'solid' => [
                'primary'   => ['background' => 'var(--zayne-color-primary)',   'color' => 'var(--zayne-color-primary-content)'],
                'secondary' => ['background' => 'var(--zayne-color-secondary)', 'color' => 'var(--zayne-color-secondary-content)'],
                'danger'    => ['background' => 'var(--zayne-color-danger)',     'color' => 'var(--zayne-color-danger-content)'],
                'success'   => ['background' => 'var(--zayne-color-success)',    'color' => 'var(--zayne-color-success-content)'],
                'warning'   => ['background' => 'var(--zayne-color-warning)',    'color' => 'var(--zayne-color-warning-content)'],
                'info'      => ['background' => 'var(--zayne-color-info)',       'color' => 'var(--zayne-color-info-content)'],
                'base'      => ['background' => 'var(--zayne-color-base-200)',   'color' => 'var(--zayne-color-base-content)'],
            ],
            'soft' => [
                'primary'   => ['background' => 'color-mix(in oklch, var(--zayne-color-primary)   15%, transparent)', 'color' => 'var(--zayne-color-primary)'],
                'secondary' => ['background' => 'color-mix(in oklch, var(--zayne-color-secondary) 15%, transparent)', 'color' => 'var(--zayne-color-secondary)'],
                'danger'    => ['background' => 'color-mix(in oklch, var(--zayne-color-danger)     15%, transparent)', 'color' => 'var(--zayne-color-danger)'],
                'success'   => ['background' => 'color-mix(in oklch, var(--zayne-color-success)    15%, transparent)', 'color' => 'var(--zayne-color-success)'],
                'warning'   => ['background' => 'color-mix(in oklch, var(--zayne-color-warning)    15%, transparent)', 'color' => 'var(--zayne-color-warning)'],
                'info'      => ['background' => 'color-mix(in oklch, var(--zayne-color-info)       15%, transparent)', 'color' => 'var(--zayne-color-info)'],
                'base'      => ['background' => 'var(--zayne-color-base-200)',                                         'color' => 'var(--zayne-color-base-content)'],
            ],
            'outline' => [
                'primary'   => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)',      'border' => '1px solid var(--zayne-color-primary)'],
                'secondary' => ['background' => 'transparent', 'color' => 'var(--zayne-color-secondary)',    'border' => '1px solid var(--zayne-color-secondary)'],
                'danger'    => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)',        'border' => '1px solid var(--zayne-color-danger)'],
                'success'   => ['background' => 'transparent', 'color' => 'var(--zayne-color-success)',       'border' => '1px solid var(--zayne-color-success)'],
                'warning'   => ['background' => 'transparent', 'color' => 'var(--zayne-color-warning)',       'border' => '1px solid var(--zayne-color-warning)'],
                'info'      => ['background' => 'transparent', 'color' => 'var(--zayne-color-info)',          'border' => '1px solid var(--zayne-color-info)'],
                'base'      => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'border' => '1px solid var(--zayne-color-base-border)'],
            ],
            'ghost' => [
                'primary'   => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)'],
                'danger'    => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)'],
                'base'      => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)'],
            ],
            'link' => [
                'primary'   => ['background' => 'transparent', 'color' => 'var(--zayne-color-primary)',      'text-decoration' => 'underline'],
                'danger'    => ['background' => 'transparent', 'color' => 'var(--zayne-color-danger)',        'text-decoration' => 'underline'],
                'base'      => ['background' => 'transparent', 'color' => 'var(--zayne-color-base-content)', 'text-decoration' => 'underline'],
            ],
        ];

        $resolved = $variantStyles[$this->variant][$this->color]
            ?? $variantStyles[$this->variant]['base']
            ?? $variantStyles['solid']['primary'];

        $base = [
            'padding'       => $this->padding,
            'border-radius' => $this->radius,
            'gap'           => $this->gap,
            'box-shadow'    => $this->shadow,
            'margin'        => $this->margin,
            'border-width'  => $this->border,
            'border-color'  => $this->bordercolor,
        ];

        $this->style = Zayne::styleString(array_merge($base, $resolved));
    }

    public function render(): View
    {
        return view('zayne::button');
    }
}
```

---

## BLADE TEMPLATE RULES

### Layout / Data — direct prop output in style=""

```blade
{{-- stubs/resources/views/components/zayne/layout/header.blade.php --}}
<header
    class="zayne-header"
    style="
        background:          {{ $background }};
        box-shadow:          {{ $shadow }};
        padding:             {{ $padding }};
        gap:                 {{ $gap }};
        margin:              {{ $margin }};
        border-width:        {{ $border }};
        border-top-width:    {{ $bordertop }};
        border-bottom-width: {{ $borderbottom }};
        border-color:        {{ $bordercolor }};
    "
    {{ $attributes }}
>
    @isset($left)
        <div class="zayne-header-left">{{ $left }}</div>
    @endisset

    <div class="zayne-header-center">{{ $slot }}</div>

    @isset($right)
        <div class="zayne-header-right">{{ $right }}</div>
    @endisset
</header>
```

### Action / Visual / Feed / Form — compiled $style

```blade
{{-- stubs/resources/views/components/zayne/button.blade.php --}}
@php
    $tag = $href ? 'a' : 'button';
@endphp

<{{ $tag }}
    class="zayne-button zayne-button--{{ $size }}"
    style="{{ $style }}"
    @if($href) href="{{ $href }}" @endif
    @if(!$href) type="button" @endif
    {{ $attributes }}
>
    @isset($iconslot)
        <span class="zayne-button-icon">{{ $iconslot }}</span>
    @endisset

    @if($slot->isNotEmpty())
        <span class="zayne-button-label">{{ $slot }}</span>
    @endif

    @isset($trailing)
        <span class="zayne-button-trailing">{{ $trailing }}</span>
    @endisset
</{{ $tag }}>
```

### Overlay — Alpine x-data by name from zayne.js

```blade
{{-- stubs/resources/views/components/zayne/modal.blade.php --}}
<div
    x-data="zayneModal()"
    class="zayne-modal-root"
    {{ $attributes->except(['class', 'style']) }}
>
    @isset($trigger)
        <div x-on:click="open = true">{{ $trigger }}</div>
    @endisset

    <div
        x-show="open"
        x-cloak
        x-on:click="open = false"
        x-bind:class="open ? 'zayne-backdrop-enter' : 'zayne-backdrop-leave'"
        class="zayne-backdrop"
    ></div>

    <div
        x-show="open"
        x-cloak
        x-trap="open"
        x-bind:class="open ? 'zayne-modal-enter' : 'zayne-modal-leave'"
        class="zayne-modal-panel"
        style="{{ $style }}"
    >
        {{ $slot }}
    </div>
</div>
```

### Root Layout — x-data by name from zayne.js

```blade
{{-- stubs/resources/views/components/zayne/layout/main.blade.php --}}
<div
    class="zayne-layout"
    x-data="zayneLayout()"
>
    @isset($sidebar)
        <aside
            class="zayne-sidebar"
            x-bind:class="{ 'is-collapsed': sidebarCollapsed, 'is-open': mobileOpen }"
        >
            {{ $sidebar }}
        </aside>
    @endisset

    <div class="zayne-main-wrapper">
        @isset($header)
            {{ $header }}
        @endisset

        <main class="zayne-main">{{ $slot }}</main>
    </div>

    <div
        class="zayne-mobile-backdrop"
        x-show="mobileOpen"
        x-cloak
        x-on:click="mobileOpen = false"
        x-bind:class="mobileOpen ? 'zayne-backdrop-enter' : 'zayne-backdrop-leave'"
    ></div>

    <button
        class="zayne-mobile-toggle"
        x-on:click="mobileOpen = !mobileOpen"
        aria-label="Toggle navigation"
    >
        <svg x-show="!mobileOpen" xmlns="http://www.w3.org/2000/svg" width="20" height="20"
            viewBox="0 0 24 24" fill="none" stroke="currentColor"
            stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="3" y1="6"  x2="21" y2="6"/>
            <line x1="3" y1="12" x2="21" y2="12"/>
            <line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
        <svg x-show="mobileOpen" x-cloak xmlns="http://www.w3.org/2000/svg" width="20" height="20"
            viewBox="0 0 24 24" fill="none" stroke="currentColor"
            stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="18" y1="6"  x2="6"  y2="18"/>
            <line x1="6"  y1="6"  x2="18" y2="18"/>
        </svg>
    </button>
</div>
```

---

## SLOT NAMES — FIXED, NEVER INVENT NEW ONES

```
Layout slots:   $left, $slot, $right
Content slots:  $header, $slot, $footer, $sidebar
Icon slots:     $iconslot
Action slots:   $trailing
Overlay slots:  $trigger, $slot
```

---

## zayne.js — ALL ALPINE DEFINITIONS

```js
// stubs/resources/js/zayne.js
// All Alpine.data() definitions for ZayneUI
// Never use inline x-data in Blade — always reference by name

document.addEventListener('alpine:init', () => {

    Alpine.data('zayneLayout', () => ({
        sidebarCollapsed: false,
        mobileOpen: false,
    }))

    Alpine.data('zayneModal', () => ({
        open: false,
    }))

    Alpine.data('zayneDrawer', () => ({
        open: false,
    }))

    Alpine.data('zayneDropdown', () => ({
        open: false,
    }))

    Alpine.data('zayneTooltip', () => ({
        open: false,
    }))

    Alpine.data('zaynePopover', () => ({
        open: false,
    }))

})
```

---

## zayne.css — THREE SECTIONS, ONE FILE

```css
/* ============================================================
|  zayne.css — ZayneUI Design System
|  THREE SECTIONS:
|  1. TOKENS     ← ✅ Editable by developers
|  2. STRUCTURE  ← ❌ Locked
|  3. ANIMATIONS ← ❌ Locked
============================================================ */


/* ============================================================
|  SECTION 1 — TOKENS
|  Edit this section to customize your theme.
|  Toggle themes by adding class to <html>:
|    class="light"  class="dark"  class="abyss"
============================================================ */

:root {
    --zayne-radius-box:      10px;
    --zayne-radius-field:    10px;
    --zayne-radius-selector: 4px;
    --zayne-border-box:      1px;
    --zayne-border-field:    1px;
    --zayne-border-selector: 2px;
    --zayne-size-field:      40px;
    --zayne-size-selector:   20px;
    --zayne-shadow:          0 8px 10px rgba(0, 0, 0, 0.08);
    --zayne-gap-button:      0.5rem;
    --zayne-padding-header:  0 1rem;
    --zayne-gap-header:      0;
}

.light {
    --zayne-color-base-100:           oklch(100% 0.00011 271.152);
    --zayne-color-base-200:           oklch(97.015% 0.00011 271.152);
    --zayne-color-base-300:           oklch(90.67% 0.0001 271.152);
    --zayne-color-base-content:       oklch(18% 0.005 271);
    --zayne-color-base-content-muted: oklch(59.31% 0.00007 271.152);
    --zayne-color-base-border:        oklch(82% 0.004 271);
    --zayne-color-base-hover:         oklch(91% 0.003 271);

    --zayne-color-primary:           oklch(98% 0.002 271);
    --zayne-color-primary-content:   oklch(28% 0.01 271);
    --zayne-color-secondary:         oklch(85% 0.199 91.936);
    --zayne-color-secondary-content: oklch(25% 0.05 91);
    --zayne-color-accent:            oklch(87% 0.169 91.605);
    --zayne-color-accent-content:    oklch(38% 0.063 188.416);

    --zayne-color-danger:          oklch(55% 0.237 25.331);
    --zayne-color-danger-content:  oklch(97% 0.01 25);
    --zayne-color-success:         oklch(48% 0.15 149.579);
    --zayne-color-success-content: oklch(97% 0.01 149);
    --zayne-color-warning:         oklch(81.79% 0.17046 77.947);
    --zayne-color-warning-content: oklch(15% 0.05 84);
    --zayne-color-info:            oklch(52% 0.14 232.661);
    --zayne-color-info-content:    oklch(97% 0.01 232);

    --zayne-custom-header:                      oklch(100% 0.00011 271.152);
    --zayne-custom-sidebar:                     #ffffff;
    --zayne-custom-sidebar-content:             #222222;
    --zayne-custom-sidebar-item-bg:             #24242409;
    --zayne-custom-sidebar-item-bg-hover:       #ffbf00;
    --zayne-custom-sidebar-item-bg-active:      rgb(6, 27, 57);
    --zayne-custom-sidebar-item-content:        #ffffff;
    --zayne-custom-sidebar-item-content-hover:  #0f0f0f;
    --zayne-custom-sidebar-item-content-active: #ffffff;

    --zayne-shadow:               0 8px 10px rgba(0, 0, 0, 0.08);
    --zayne-custom-layout-shadow: 0 8px 10px rgba(0, 0, 0, 0.08);
}

.dark {
    --zayne-color-base-100:           oklch(18% 0.005 271);
    --zayne-color-base-200:           oklch(22% 0.005 271);
    --zayne-color-base-300:           oklch(27% 0.005 271);
    --zayne-color-base-content:       oklch(88% 0.005 271);
    --zayne-color-base-content-muted: oklch(55% 0.005 271);
    --zayne-color-base-border:        oklch(32% 0.005 271);
    --zayne-color-base-hover:         oklch(30% 0.005 271);

    --zayne-color-primary:           oklch(14% 0.005 271);
    --zayne-color-primary-content:   oklch(92% 0.005 271);
    --zayne-color-secondary:         oklch(85% 0.199 91.936);
    --zayne-color-secondary-content: oklch(25% 0.05 91);

    --zayne-color-danger:          oklch(63% 0.237 25.331);
    --zayne-color-danger-content:  oklch(96% 0.01 25);
    --zayne-color-success:         oklch(72% 0.219 149.579);
    --zayne-color-success-content: oklch(96% 0.01 149);
    --zayne-color-warning:         oklch(82% 0.189 84.429);
    --zayne-color-warning-content: oklch(25% 0.05 84);
    --zayne-color-info:            oklch(74% 0.16 232.661);
    --zayne-color-info-content:    oklch(96% 0.01 232);

    --zayne-custom-header:                      oklch(22% 0.005 271);
    --zayne-custom-sidebar:                     oklch(15% 0.005 271);
    --zayne-custom-sidebar-content:             oklch(88% 0.005 271);
    --zayne-custom-sidebar-item-bg:             rgba(255, 255, 255, 0.04);
    --zayne-custom-sidebar-item-bg-hover:       rgba(255, 255, 255, 0.08);
    --zayne-custom-sidebar-item-bg-active:      rgba(255, 255, 255, 0.12);
    --zayne-custom-sidebar-item-content:        oklch(70% 0.005 271);
    --zayne-custom-sidebar-item-content-hover:  oklch(95% 0.005 271);
    --zayne-custom-sidebar-item-content-active: #ffffff;

    --zayne-shadow:               0 8px 10px rgba(0, 0, 0, 0.4);
    --zayne-custom-layout-shadow: 0 8px 10px rgba(0, 0, 0, 0.08);
}

.abyss {
    --zayne-color-base-100:           oklch(20% 0.08 209);
    --zayne-color-base-200:           oklch(15% 0.08 209);
    --zayne-color-base-300:           oklch(10% 0.08 209);
    --zayne-color-base-content:       oklch(99.226% 0.00608 75.126);
    --zayne-color-base-content-muted: oklch(60.409% 0.0418 73.346);
    --zayne-color-base-border:        oklch(30% 0.08 209);
    --zayne-color-base-hover:         oklch(25% 0.08 209);

    --zayne-color-primary:           oklch(50% 0.2653 125);
    --zayne-color-primary-content:   oklch(92% 0.2653 125);
    --zayne-color-secondary:         oklch(83.27% 0.0764 298.3);
    --zayne-color-secondary-content: oklch(43.27% 0.0764 298.3);

    --zayne-color-danger:          oklch(65% 0.1985 24.22);
    --zayne-color-danger-content:  oklch(27% 0.1985 24.22);
    --zayne-color-success:         oklch(79% 0.209 151.711);
    --zayne-color-success-content: oklch(26% 0.065 152.934);
    --zayne-color-warning:         oklch(84.8% 0.1962 84.62);
    --zayne-color-warning-content: oklch(44.8% 0.1962 84.62);
    --zayne-color-info:            oklch(74% 0.16 232.661);
    --zayne-color-info-content:    oklch(29% 0.066 243.157);

    --zayne-custom-header:          oklch(15% 0.08 209);
    --zayne-custom-sidebar:         oklch(10% 0.08 209);
    --zayne-radius-selector:        2rem;
    --zayne-radius-field:           0.25rem;
    --zayne-radius-box:             0.5rem;
    --zayne-shadow:                 0 8px 10px rgba(0, 0, 0, 0.4);
}


/* ============================================================
|  SECTION 2 — STRUCTURE
|  Do not edit. Layout and component structural hooks.
============================================================ */

/* ── Easing ─────────────────────────────────────────────── */
:root {
    --ease-out-smooth: cubic-bezier(0.16, 1, 0.3, 1);
    --ease-in-smooth:  cubic-bezier(0.4, 0, 1, 1);
    --ease-spring:     cubic-bezier(0.34, 1.56, 0.64, 1);
}

/* ── Root Layout ────────────────────────────────────────── */
.zayne-layout {
    display: grid;
    grid-template-columns: auto 1fr;
    grid-template-rows: 1fr;
    min-height: 100dvh;
    grid-template-areas: "sidebar main";
}

.zayne-sidebar {
    grid-area: sidebar;
    width: 260px;
    transition: width 260ms var(--ease-out-smooth);
    overflow: hidden;
    background: var(--zayne-custom-sidebar);
    color: var(--zayne-custom-sidebar-content);
}

.zayne-sidebar.is-collapsed { width: 64px; }

.zayne-main-wrapper {
    grid-area: main;
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
    overflow: hidden;
}

.zayne-main {
    flex: 1;
    overflow-y: auto;
    background: var(--zayne-color-base-100);
}

/* ── Header ─────────────────────────────────────────────── */
.zayne-header {
    display: flex;
    flex-direction: row;
    align-items: center;
    min-height: var(--zayne-size-field);
    background: var(--zayne-custom-header);
    border-style: solid;
}

.zayne-header-left   { flex-shrink: 0; display: flex; align-items: center; }
.zayne-header-center { flex: 1;        display: flex; align-items: center; min-width: 0; }
.zayne-header-right  { flex-shrink: 0; display: flex; align-items: center; margin-left: auto; }

/* ── Button ─────────────────────────────────────────────── */
.zayne-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 500;
    cursor: pointer;
    border: none;
    text-decoration: none;
    flex-shrink: 0;
    transition: filter 150ms ease, transform 100ms ease, box-shadow 150ms ease;
    white-space: nowrap;
    border-style: solid;
    border-width: 0;
}

.zayne-button:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 2px;
}

.zayne-button:not([disabled]):hover  { filter: brightness(1.08); }
.zayne-button:not([disabled]):active { transform: translateY(1px); filter: brightness(0.95); }
.zayne-button[disabled]              { opacity: 0.5; cursor: not-allowed; pointer-events: none; }

.zayne-button--xs { height: 1.5rem;  font-size: 0.75rem;  padding: 0 0.5rem; border-radius: var(--zayne-radius-selector); }
.zayne-button--sm { height: 2rem;    font-size: 0.875rem; padding: 0 0.75rem; border-radius: var(--zayne-radius-field); }
.zayne-button--md { height: 2.5rem;  font-size: 0.875rem; padding: 0 1rem;   border-radius: var(--zayne-radius-field); }
.zayne-button--lg { height: 3rem;    font-size: 1rem;     padding: 0 1.25rem; border-radius: var(--zayne-radius-field); }
.zayne-button--xl { height: 3.5rem;  font-size: 1.125rem; padding: 0 1.5rem; border-radius: var(--zayne-radius-box); }

.zayne-button--xs.zayne-button--square { width: 1.5rem;  padding: 0; }
.zayne-button--sm.zayne-button--square { width: 2rem;    padding: 0; }
.zayne-button--md.zayne-button--square { width: 2.5rem;  padding: 0; }
.zayne-button--lg.zayne-button--square { width: 3rem;    padding: 0; }
.zayne-button--xl.zayne-button--square { width: 3.5rem;  padding: 0; }

.zayne-button-icon  { display: flex; align-items: center; flex-shrink: 0; }
.zayne-button-label { display: inline-flex; align-items: center; }

/* ── Badge ──────────────────────────────────────────────── */
.zayne-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-weight: 500;
    white-space: nowrap;
    border-style: solid;
    border-width: 0;
}

.zayne-badge--sm { height: 1.25rem; font-size: 0.7rem;  padding: 0 0.4rem;  border-radius: var(--zayne-radius-selector); }
.zayne-badge--md { height: 1.5rem;  font-size: 0.75rem; padding: 0 0.5rem;  border-radius: var(--zayne-radius-selector); }
.zayne-badge--lg { height: 1.75rem; font-size: 0.8rem;  padding: 0 0.625rem; border-radius: var(--zayne-radius-selector); }

/* ── Icon Wrapper ───────────────────────────────────────── */
.zayne-icon-wrapper {
    width: 38px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

/* ── Overlay Backdrop ───────────────────────────────────── */
.zayne-backdrop {
    position: fixed;
    inset: 0;
    z-index: 40;
    background: rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
}

/* ── Modal ──────────────────────────────────────────────── */
.zayne-modal-root { position: relative; }

.zayne-modal-panel {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 50;
    background: var(--zayne-color-base-100);
    border-radius: var(--zayne-radius-box);
    box-shadow: var(--zayne-shadow);
    min-width: 320px;
    max-width: 90vw;
    max-height: 90dvh;
    overflow-y: auto;
    padding: 1.5rem;
}

/* ── Drawer ─────────────────────────────────────────────── */
.zayne-drawer-panel {
    position: fixed;
    top: 0;
    left: 0;
    height: 100%;
    z-index: 50;
    background: var(--zayne-color-base-100);
    box-shadow: var(--zayne-shadow);
    overflow-y: auto;
}

/* ── Card ───────────────────────────────────────────────── */
.zayne-card {
    background: var(--zayne-color-base-100);
    border-radius: var(--zayne-radius-box);
    border: var(--zayne-border-box) solid var(--zayne-color-base-border);
}

/* ── Input ──────────────────────────────────────────────── */
.zayne-input {
    display: flex;
    align-items: center;
    width: 100%;
    height: var(--zayne-size-field);
    border: var(--zayne-border-field) solid var(--zayne-color-base-border);
    border-radius: var(--zayne-radius-field);
    background: var(--zayne-color-base-100);
    color: var(--zayne-color-base-content);
    transition: border-color 150ms ease, box-shadow 150ms ease;
}

.zayne-input:focus-within {
    border-color: var(--zayne-color-primary);
    outline: none;
}

/* ── Progress ───────────────────────────────────────────── */
.zayne-progress {
    width: 100%;
    height: 8px;
    border-radius: 999px;
    background: var(--zayne-color-base-300);
    overflow: hidden;
}

.zayne-progress-bar {
    height: 100%;
    border-radius: 999px;
    transition: width 300ms ease;
}

/* ── Sidebar Nav Items ──────────────────────────────────── */
.zayne-sidebar-label {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0 1rem;
    color: var(--zayne-custom-sidebar-content);
    opacity: 0.5;
    transition: opacity 200ms;
}

.zayne-sidebar.is-collapsed .zayne-sidebar-label { opacity: 0; pointer-events: none; }

.zayne-navitem {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.5rem 0.75rem;
    border-radius: var(--zayne-radius-field);
    cursor: pointer;
    transition: background 150ms ease, color 150ms ease;
    text-decoration: none;
    color: var(--zayne-custom-sidebar-item-content);
    background: var(--zayne-custom-sidebar-item-bg);
    font-size: 0.875rem;
    font-weight: 500;
    border: none;
    width: 100%;
}

.zayne-navitem:hover  { background: var(--zayne-custom-sidebar-item-bg-hover);  color: var(--zayne-custom-sidebar-item-content-hover); }
.zayne-navitem.active { background: var(--zayne-custom-sidebar-item-bg-active); color: var(--zayne-custom-sidebar-item-content-active); }

/* ── Mobile ─────────────────────────────────────────────── */
@media (max-width: 768px) {
    .zayne-layout {
        grid-template-columns: 1fr;
        grid-template-areas: "main";
    }

    .zayne-sidebar {
        position: fixed;
        top: 0;
        left: 0;
        height: 100%;
        z-index: 50;
        transform: translateX(-100%);
        transition: transform 260ms var(--ease-out-smooth);
        width: 260px !important;
    }

    .zayne-sidebar.is-open { transform: translateX(0); }

    .zayne-mobile-toggle {
        display: flex;
        position: fixed;
        bottom: 1.5rem;
        left: 1.5rem;
        z-index: 60;
        width: 48px;
        height: 48px;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        border: none;
        cursor: pointer;
        box-shadow: var(--zayne-custom-layout-shadow);
        background: var(--zayne-color-primary);
        color: var(--zayne-color-primary-content);
    }
}

@media (min-width: 769px) {
    .zayne-mobile-toggle   { display: none; }
    .zayne-mobile-backdrop { display: none; }
}


/* ============================================================
|  SECTION 3 — ANIMATIONS
|  Do not edit. Keyframes and enter/leave classes.
============================================================ */

@keyframes zayne-modal-in    { from { opacity: 0; transform: translate(-50%, -48%) scale(0.97); } to { opacity: 1; transform: translate(-50%, -50%) scale(1); } }
@keyframes zayne-modal-out   { from { opacity: 1; transform: translate(-50%, -50%) scale(1);    } to { opacity: 0; transform: translate(-50%, -52%) scale(0.97); } }
@keyframes zayne-fade-in     { from { opacity: 0; } to { opacity: 1; } }
@keyframes zayne-fade-out    { from { opacity: 1; } to { opacity: 0; } }
@keyframes zayne-drawer-in   { from { transform: translateX(-100%); } to { transform: translateX(0); } }
@keyframes zayne-drawer-out  { from { transform: translateX(0);     } to { transform: translateX(-100%); } }
@keyframes zayne-slide-down  { from { opacity: 0; transform: translateY(-6px); } to { opacity: 1; transform: translateY(0); } }
@keyframes zayne-slide-up    { from { opacity: 1; transform: translateY(0);    } to { opacity: 0; transform: translateY(-6px); } }

.zayne-modal-enter    { animation: zayne-modal-in   260ms var(--ease-out-smooth) forwards; }
.zayne-modal-leave    { animation: zayne-modal-out  140ms var(--ease-in-smooth)  backwards; }
.zayne-backdrop-enter { animation: zayne-fade-in    180ms var(--ease-out-smooth) forwards; }
.zayne-backdrop-leave { animation: zayne-fade-out   120ms var(--ease-in-smooth)  backwards; }
.zayne-drawer-enter   { animation: zayne-drawer-in  260ms var(--ease-out-smooth) forwards; }
.zayne-drawer-leave   { animation: zayne-drawer-out 200ms var(--ease-in-smooth)  backwards; }
.zayne-dropdown-enter { animation: zayne-slide-down 180ms var(--ease-out-smooth) forwards; }
.zayne-dropdown-leave { animation: zayne-slide-up   120ms var(--ease-in-smooth)  backwards; }
```

---

## PROP RULES

### Props with a Zayne default — never `unset`
```php
public string $background = 'var(--zayne-custom-header)',
public string $shadow     = 'var(--zayne-shadow)',
public string $radius     = 'var(--zayne-radius-field)',
```

### Props off by default — always `'unset'`
```php
public string $border       = 'unset',
public string $bordertop    = 'unset',
public string $borderbottom = 'unset',
public string $borderleft   = 'unset',
public string $borderright  = 'unset',
public string $bordercolor  = 'unset',
public string $margin       = 'unset',
public string $margintop    = 'unset',
public string $marginbottom = 'unset',
public string $marginleft   = 'unset',
public string $marginright  = 'unset',
```

Never use PHP `null`. Never use the string `'null'`. Only `'unset'`.

---

## USAGE EXAMPLES — ALL MUST WORK AFTER composer require

```blade
{{-- Zero config --}}
<zayne:button />

{{-- Variant + color --}}
<zayne:button variant="outline" color="danger" />

{{-- Size --}}
<zayne:button variant="solid" color="primary" size="lg" />

{{-- With icon slot --}}
<zayne:button variant="solid" color="primary">
    <x-slot:iconslot><svg .../></x-slot:iconslot>
    Save
</zayne:button>

{{-- As link --}}
<zayne:button href="/dashboard" variant="ghost" color="base">Dashboard</zayne:button>

{{-- Badge --}}
<zayne:badge variant="soft" color="success">Active</zayne:badge>

{{-- Modal --}}
<zayne:modal>
    <x-slot:trigger>
        <zayne:button>Open</zayne:button>
    </x-slot:trigger>
    Modal content here
</zayne:modal>

{{-- Header with border --}}
<zayne:header borderbottom="1px" bordercolor="var(--zayne-color-base-border)" />

{{-- Full layout --}}
<zayne:layout>
    <x-slot:sidebar>
        <zayne:sidebar.brand name="MyApp" />
        <zayne:sidebar.navitem href="/dashboard" icon="...">Dashboard</zayne:sidebar.navitem>
    </x-slot:sidebar>
    <x-slot:header>
        <zayne:header>
            <x-slot:right>
                <zayne:button variant="ghost" color="base" size="sm">Profile</zayne:button>
            </x-slot:right>
        </zayne:header>
    </x-slot:header>
    Page content here
</zayne:layout>
```

---

## HARD RULES — CODEX MUST FOLLOW THESE

```
❌  No Tailwind classes anywhere
❌  No hardcoded hex, rgb, or hsl color values in PHP classes
❌  No logic or calculations in Blade
❌  No base class (ZayneComponent) — each component extends Component directly
❌  No traits (HasColorVariants, HasSemanticColors) — variant map inline in mount()
❌  No raw prop values in style= on variant components — always goes through $style
❌  No variants on Layout or Data components
❌  No mount() on Layout or Data components
❌  No $tag prop — href present = <a>, no href = <button> (resolved in Blade)
❌  No @if for slot checks — always use @isset
❌  No inline x-data in Blade — always reference zayne.js by name
❌  No Alpine outside Overlay components or root layout
❌  No 'null' string as prop default — use 'unset'
❌  No PHP null as prop default
❌  No configurable mobile breakpoint — hardcoded 768px in zayne.css section 2
❌  No stub copying in InstallCommand — package works without publishing
❌  No TailwindMerge dependency

✅  Every default prop references zayne.css tokens via var()
✅  Off-by-default props use CSS 'unset' as default value
✅  Layout / Data: props output directly in Blade style=""
✅  Action / Visual / Feed / Form / Overlay: mount() builds $style via Zayne::styleString()
✅  mount() exists only for variant resolution — nothing else
✅  Variant map lives inline inside mount() — no external files
✅  All Alpine x-data definitions live in zayne.js — Blade uses x-data="zayneModal()"
✅  Every component passes $attributes through untouched
✅  Slot names are consistent — $left $right $header $footer $sidebar $iconslot $trigger $trailing
✅  Pseudo-classes :hover :focus-visible :active live in zayne.css section 2
✅  Animations and keyframes live in zayne.css section 3
✅  Mobile breakpoint hardcoded at 768px in zayne.css section 2
✅  One PHP class + one Blade file per component, always
✅  PHP classes live in src/Components/ — package-owned, no install required
✅  Blade views live in stubs/resources/views/components/zayne/
✅  ZayneServiceProvider registers Blade::componentNamespace('Zayne\\UI\\Components', 'zayne')
✅  User-published path registered before package path — user always wins
✅  zayne:publish copies both PHP class + Blade view, rewrites namespace for user
✅  @zayneStyles serves zayne.css via route with version hash
✅  @zayneScripts serves zayne.js via route with version hash
✅  @zayneAppearance injects theme persistence script
```

---

## WHAT CODEX NEEDS TO BUILD

Delete everything in the current `src/` and `stubs/` directories.
Rebuild from scratch following this guide exactly.

### Files to create:

**src/ — package infrastructure**
- `src/Zayne.php`
- `src/ZayneManager.php`
- `src/ZayneServiceProvider.php`
- `src/ZayneTagCompiler.php`
- `src/ZayneAssetManager.php`
- `src/Console/InstallCommand.php`
- `src/Console/PublishCommand.php`

**src/Components/ — PHP classes, one per component**
- All Layout, Header, Sidebar sub-components
- All UI components: Button, Badge, Alert, Modal, Drawer, Dropdown, Tooltip, Popover, Input, Select, Checkbox, Toggle, Avatar, Card, Progress

**stubs/resources/css/zayne.css** — tokens + structure + animations, one file

**stubs/resources/js/zayne.js** — all Alpine.data() definitions

**stubs/resources/views/components/zayne/** — one blade file per component

**stubs/resources/views/layouts/** — app and auth layout shells

**composer.json** — update autoload, extra.laravel providers/aliases
